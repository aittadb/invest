# Investor App Implementation Plan

This file is the flat graph of accepted, unfinished implementation work. Stable `TASK-NNN` identifiers must not be renumbered or reused. List order is a topological reading aid, not a serial execution order.

Each task should be the smallest coherent increment that can finish in one focused commit with an objective pass/fail definition of done. `Depends on` names only direct prerequisites. A dependency recorded in `CHANGELOG.md` is satisfied; one still present here is not. A task whose dependencies are satisfied and which has no external blocker may proceed in parallel with every other ready task, preferably in an isolated Git worktree. Do not combine independent routes, storage contracts, security controls, UI flows, migrations, or live proof matrices. Completed tasks move to `CHANGELOG.md` with evidence and are removed from this file.

- [ ] TASK-053: Reject compound SQL before Sites migration splitting. Depends on: `TASK-049`. DoD: migration staging detects every unquoted SQLite `TRIGGER` token despite comments or TEMP modifiers, refuses to emit breakpoints for compound SQL, executes each emitted breakpoint segment independently in tests, and focused tests plus lint, typecheck, and build pass.

- [ ] TASK-054: Permit the exact OAuth authorization origin through the connection form CSP. Depends on: `TASK-048`, `TASK-050`. DoD: the owner connection resource's HTML CSP allows form submission redirects only to self and the exact configured HTTPS authorization origin; every other external origin remains blocked; JSON, callback, and error responses do not widen browser form targets unnecessarily; route tests plus a real-browser redirect check prove the flow reaches the provider; and lint, typecheck, and build pass.

- [ ] TASK-051: Compose the hosted owner OAuth proof capability. Depends on: `TASK-048`, `TASK-049`, `TASK-050`, `TASK-052`, `TASK-053`, `TASK-054`. DoD: the production Worker injects the owner connection route only when complete OAuth configuration, D1, and cryptographic capabilities are present; initiation and callback use the exact configured issuer, redirect URI, scopes, durable single-use claim, and closed proof sink; incomplete deployments keep the route absent, no credential enters logs or responses, and focused composition tests plus full validation pass.

- [ ] TASK-030: Prove the configured AittaDB OAuth client path. Depends on: `TASK-051`. Blocked by: an active acceptance operator must provision the confidential client and complete the acceptance sign-in once the hosted callback is deployed. DoD: the selected AittaDB deployment advertises Authorization Code with PKCE, a confidential Investor App client has only the required storage scopes and exact redirect URIs, secrets remain hosted-only, and a development callback obtains and validates a scoped token without logging credentials.
