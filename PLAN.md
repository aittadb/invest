# Investor App Implementation Plan

This file is the flat graph of accepted, unfinished implementation work. Stable `TASK-NNN` identifiers must not be renumbered or reused. List order is a topological reading aid, not a serial execution order.

Each task should be the smallest coherent increment that can finish in one focused commit with an objective pass/fail definition of done. `Depends on` names only direct prerequisites. A dependency recorded in `CHANGELOG.md` is satisfied; one still present here is not. A task whose dependencies are satisfied and which has no external blocker may proceed in parallel with every other ready task, preferably in an isolated Git worktree. Do not combine independent routes, storage contracts, security controls, UI flows, migrations, or live proof matrices. Completed tasks move to `CHANGELOG.md` with evidence and are removed from this file.

- [ ] TASK-168: Extract owner indication review collection persistence into a storage-owned module. Depends on: `TASK-171`. DoD: `StorageOwnerIndicationReviewCollectionRepository` and only its cohesive collection helpers move from the development-named indication repository to one storage-owned module behind the existing service contract; all production consumers use the new boundary, owner authorization, sealed cursor binding, page/read bounds, terminal/current/lease verification, corruption handling, and non-disclosing failures remain unchanged, architecture documentation and focused collection/moderation tests are current, and full validation passes.
- [ ] TASK-131: Prove the production AittaDB `StorageAdapter` contract against hosted acceptance. Depends on: `TASK-154`, `TASK-162`. Blocked by: the AittaDB project must publish the accepted atomic storage transaction action and fresh server-authenticated disposable-proof assertion on its acceptance deployment. DoD: the disposable deployment advertises the accepted transaction contract and one least-privilege service-client-bound adapter passes the unchanged hosted authorization, consistency, uniqueness, history, pagination, idempotency, quota, rollback, restart, and non-disclosure matrix; retained evidence is redacted, documentation is current, and full validation passes.
- [ ] TASK-132: Dispose of hosted adapter proof state. Depends on: `TASK-131`. DoD: every TASK-131 synthetic fixture and proof credential is inventoried and either removed or intentionally retained outside Git with one bounded purpose and expiry; unrelated acceptance data, clients, settings, and production state remain unchanged, retained evidence contains no credential or private value, and full validation passes.
- [ ] TASK-112: Prove hosted browser and Worker credential non-exposure. Depends on: `TASK-111`, `TASK-131`. DoD: the exact acceptance deployment's browser network, HTML, hypermedia JSON, downloads, redirects, CSP, errors, cache headers, and bounded Worker logs expose no AittaDB credential, token, service-client value, private environment setting, owner identity, or backend response detail across public, participant, owner, success, and failure paths; retained evidence is redacted and bound to the deployed source version.
- [ ] TASK-113: Prove least-privilege AittaDB service-client authorization. Depends on: `TASK-131`. DoD: the acceptance service client has no browser origin or redirect URI, owns only the canonical storage scopes and namespace needed by Investor App, anonymous and unrelated clients receive the same non-disclosing denial, and metadata plus negative read/write/delete tests retain no client identifier, credential, token, namespace key, or participant data.
- [ ] TASK-137: Prove hosted public onboarding and package access. Depends on: `TASK-093`, `TASK-112`. DoD: one exact acceptance version proves the signed-out campaign, first registration, immediate package access, current acknowledgment, and registered return through equivalent HTML and hypermedia using namespaced synthetic data; unavailable actions, credentials, and foreign data are not disclosed.
- [ ] TASK-138: Prove the hosted founder-application journey. Depends on: `TASK-094`, `TASK-112`, `TASK-133`, `TASK-134`. DoD: one permitted acceptance participant creates, edits, reviews, and withdraws one founder application through equivalent HTML and hypermedia, and the configured owner pages and inspects its opaque review resources; closed, stale, non-owner, foreign, and retry paths change nothing unexpectedly and disclose nothing.
- [ ] TASK-139: Prove the hosted investment and public-aggregate journey. Depends on: `TASK-074`, `TASK-096`, `TASK-112`. DoD: acceptance participants create personal and company indications, edit, withdraw, and reactivate them while the public aggregate changes exactly as configured through equivalent HTML and hypermedia; duplicate, stale, retry, non-owner, foreign, and hidden/zero aggregate paths remain consistent and non-disclosing.
- [ ] TASK-140: Prove the hosted owner campaign-setup journey. Depends on: `TASK-068`, `TASK-112`. DoD: the configured acceptance owner creates and edits one namespaced unpublished campaign through equivalent bounded HTML and hypermedia while anonymous, non-owner, stale, retry, malformed, and restart paths change nothing unexpectedly and disclose no private value.
- [ ] TASK-141: Prove the hosted owner package-version journey. Depends on: `TASK-069`, `TASK-112`. DoD: the configured acceptance owner creates, previews, publishes, and supersedes namespaced private package versions and acknowledgment requirements through equivalent bounded HTML and hypermedia while anonymous, non-owner, stale, retry, malformed, and restart paths remain safe and non-disclosing.
- [ ] TASK-148: Prove hosted two-subject and role isolation. Depends on: `TASK-137`, `TASK-138`, `TASK-139`, `TASK-140`, `TASK-141`. DoD: two namespaced participants plus the configured owner exercise each other's opaque identifiers and role-specific links across public, participant, founder, investment, package, and setup resources; every unauthorized response and advertised action is equivalent, changes no foreign state, and discloses no identity, existence, or credential.
- [ ] TASK-149: Prove hosted persistence, concurrency, and recovery. Depends on: `TASK-137`, `TASK-138`, `TASK-139`, `TASK-140`, `TASK-141`. DoD: the exact acceptance deployment preserves each MVP workflow result across Worker reconstruction, concurrent exact retry, stale conflict, commit-response loss, transient dependency failure, and configured quota boundary without duplicate history, aggregate drift, partial multi-record effects, credential disclosure, or unbounded work; rollback and recovery evidence is retained without private values.
- [ ] TASK-150: Prove hosted responsive accessibility and representation parity. Depends on: `TASK-074`, `TASK-076`, `TASK-093`, `TASK-094`, `TASK-096`, `TASK-104`, `TASK-105`, `TASK-106`, `TASK-112`, `TASK-133`, `TASK-134`. DoD: keyboard and browser checks at supported mobile and desktop viewports prove one clear heading, labeled inputs, visible focus, no overlap or horizontal overflow, stable controls, and state/action parity between HTML and versioned hypermedia for every enabled MVP public, participant, and owner resource; focused accessibility checks and full validation pass.
- [ ] TASK-151: Close the hosted pre-registration acceptance environment. Depends on: `TASK-132`, `TASK-137`, `TASK-138`, `TASK-139`, `TASK-140`, `TASK-141`, `TASK-148`, `TASK-149`, `TASK-150`. DoD: all MVP acceptance findings are resolved, the exact accepted source and environment revisions plus rollback version are recorded without secrets, every remaining synthetic fixture is removed or explicitly retained under one documented bounded purpose and expiry, credential-free logs and service health are rechecked, and full local validation passes with no production state or access change.
- [ ] TASK-120: Provision the production Investor App service client. Depends on: `TASK-151`. Blocked by: explicit owner approval for production AittaDB service-client registration. DoD: one dedicated confidential production client has no origins or redirects and only the required canonical storage scopes and Investor namespace; its credential is transferred directly to the production Sites secret boundary, metadata and negative authorization are verified without retaining identifiers or secrets, and no unrelated AittaDB setting or data changes.
- [ ] TASK-121: Configure the production Sites runtime boundary. Depends on: `TASK-120`. Blocked by: explicit owner approval for production hosted-secret and environment updates. DoD: production Sites contains the exact app origin, storage issuer and optional transport, namespace, scopes, owner identity, privacy contact, cryptographic keys, readiness flag, and service credential solely as validated hosted settings; source and retained evidence contain none, pairwise key separation and access controls pass, and no deployment or campaign mutation occurs.
- [ ] TASK-122: Deploy the unpublished production runtime. Depends on: `TASK-121`. Blocked by: explicit owner approval for an unpublished `invest.aittadb.com` deployment. DoD: the exact accepted source is saved and deployed with verified bindings, quotas, health, owner access, public unpublished state, and rollback version while creating no campaign, participant, indication, founder, aggregate, audit, or unrelated production record; scans and logs contain no secret or private setting.
- [ ] TASK-123: Configure the production campaign's public policy. Depends on: `TASK-122`. Blocked by: the owner must supply or approve the real public copy, phase, country, amount, privacy, retention, and legal-boundary settings. DoD: the configured owner enters the approved public campaign identity, copy, media, phase, country, amount, privacy, retention, founder-choice, and legal-boundary values through production owner resources, publishes no campaign, verifies every required readiness field is explicit, and retained evidence contains no participant data, credential, internal strategy, or synthetic placeholder.
- [ ] TASK-152: Configure the production private package and acknowledgment. Depends on: `TASK-123`. Blocked by: the owner must supply or approve every enabled private package section and acknowledgment requirement. DoD: the configured owner enters and previews the approved private sections and acknowledgment through production owner resources without publication; disabled or empty sections remain absent, version and material-change behavior is explicit, and retained evidence contains no package text, participant data, credential, internal strategy, or synthetic placeholder.
- [ ] TASK-124: Review the unpublished production campaign. Depends on: `TASK-152`. Blocked by: owner review of the exact production public preview and every enabled private package section. DoD: the owner confirms the campaign identity, public copy and media, non-binding boundary, privacy and retention wording, phase/country/amount rules, acknowledgment, founder choices, and every private section; no empty or synthetic content remains, the campaign stays unpublished, and only a content-free approval record is retained.
- [ ] TASK-153: Prove production monitoring and rollback readiness. Depends on: `TASK-122`. DoD: credential-free production health and bounded log checks are defined and pass for the exact unpublished revision, the recorded rollback version remains deployable with its required bindings, one non-mutating rollback rehearsal or equivalent platform proof succeeds without changing campaign publication or unrelated data, and retained evidence contains no secret or private setting.
- [ ] TASK-125: Publish the approved production campaign. Depends on: `TASK-124`, `TASK-153`. Blocked by: explicit owner approval of the exact production preview and publication action. DoD: the configured owner publishes only the reviewed immutable campaign/package revision through the guarded production action, the resulting source and environment revisions plus ready rollback version are recorded without secrets, and no merge to `main`, participant submission, unrelated data mutation, or access-setting change occurs.
- [ ] TASK-126: Verify the published production campaign. Depends on: `TASK-125`. DoD: immediate signed-out, signed-in participant, and configured-owner smoke tests prove the public campaign and role boundaries without creating synthetic submissions or reading unrelated data; monitoring and credential-free logs remain healthy, rollback is executed on any failed gate, and the verified production revision is recorded without secrets.
