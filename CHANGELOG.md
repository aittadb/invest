# Changelog

Completed `PLAN.md` tasks and retired planning umbrellas are preserved here. Entries should include the unchanged task description, decisive validation evidence, and any residual uncertainty.

## Unreleased

- **TASK-172:** Retire fully integrated refactoring task work. Depends on: none. DoD: the clean `/private/tmp/invest-task-168` worktree is removed and only the local `codex/task-167-public-campaign-presentation`, `codex/task-168-owner-indication-review`, and `codex/task-170-request-capability-composition` branches are deleted after fresh ancestry checks prove every tip is integrated into `develop`; every branch and worktree with distinct or acceptance-specific work remains registered, the final audit contains no eligible listed ref, retained evidence contains no private value, and `npm run plan:check` passes.

  Completion evidence: fresh clean-worktree and `merge-base --is-ancestor` checks proved only `/private/tmp/invest-task-168` and the three named branch tips fully integrated into `develop`. The worktree was removed before its branch, then those three local branches were deleted. The final registration retains only `develop` and the detached acceptance worktree; `codex/task-093`, `task-096`, `task-109`, both `task-110` branches, and `task-158` through `task-160` each still contain distinct work and remain untouched. The retained audit records branch names and integration state only, and `npm run plan:check` passes.

- **TASK-168:** Extract owner indication review collection persistence into a storage-owned module. Depends on: `TASK-171`. DoD: `StorageOwnerIndicationReviewCollectionRepository` and only its cohesive collection helpers move from the development-named indication repository to one storage-owned module behind the existing service contract; all production consumers use the new boundary, owner authorization, sealed cursor binding, page/read bounds, terminal/current/lease verification, corruption handling, and non-disclosing failures remain unchanged, architecture documentation and focused collection/moderation tests are current, and full validation passes.

  Completion evidence: `repositories/storage-owner-indication-review-collection-repository.ts` now owns the owner-only paginated collection, its read-only `list`/`read` boundary, configured-owner gate, sealed cursor binding, closed page and record validation, compact summary assembly, and fixed failure mapping. Production moderation and factory composition import that module directly; the development-named indication repository no longer exports or contains the collection. `storage-indication-read-codec.ts` supplies one neutral no-I/O terminal projection verifier and the typed current collection identity, preserving compact current/terminal/field/lease verification without full ancestry reconstruction. Focused collection, moderation, factory, participant-summary, corruption, cursor, authorization, non-disclosure, no-logging, and page-read-budget tests pass. Independent Sol High review found no P0/P1/P2 findings and confirmed no production collection import cycle or mutation authority. `npm run validate` passes the agent-size, plan, instance-boundary, lint, typecheck, build, runtime-secret-boundary, and full 1,272-test suite; `git diff --check` passes.

- **TASK-171:** Extract the neutral verified indication-read persistence codec. Depends on: none. DoD: one repository-internal storage-owned module defines the exact current, transition, fields, and lease decoding, key derivation, canonical document validation, and bounded materialization primitives currently shared by indication collection, detail, rejection, and participant read paths; those consumers use the narrow boundary without a runtime import cycle or widened storage authority, no mutation policy, replay, aggregate, or ownership behavior moves, current/history/fields/lease/corruption/crossed/restart/non-disclosure regression coverage is current, architecture documentation is current, and full validation passes.

  Completion evidence: `repositories/storage-indication-read-codec.ts` now owns the schema-5 indication read codec with `Pick<StorageAdapter, "read">` authority: exact current, transition, field-chunk, and active-lease validation; key derivation; bounded immutable ancestry reconstruction; and canonical document/fingerprint verification. The compatibility indication repository delegates its verified reads, while owner collection, detail, rejection, moderation, and participant paths consume the narrow storage boundary without a runtime cycle. Mutation, replay, aggregate, and ownership policy remain in their existing repositories. Focused indication regression coverage adds direct verified-codec reconstruction and retains current/history/field/lease, corruption, crossed, restart, bounded-read, and non-disclosure coverage across the affected repository suites. An independent Sol High security and correctness review found no P0/P1/P2 findings. `npm run validate` passes the agent-size, plan, instance-boundary, lint, typecheck, build, runtime-secret-boundary, and full 1,272-test suite; `git diff --check` passes.

- **TASK-167:** Extract public campaign presentation persistence into a storage-owned module. Depends on: none. DoD: `StoragePublicCampaignPresentationReader` and its complete v4/v5 public-presentation persistence codec move from the development-named campaign repository to one storage-owned module; all production consumers use the new boundary, the stable public reader contract, publication gating, atomic write verification, malformed-record failures, public non-disclosure, and current behavior remain unchanged, architecture documentation and focused campaign/public-state tests are current, and full validation passes.

  Completion evidence: commit `49c27b9` moves the production public-presentation reader, its exact schema-4/schema-5 codec, public projection contracts, and write mutation builder to `repositories/storage-public-campaign-presentation.ts`. Production state and factory composition now import the storage-owned reader; development compatibility and stable type re-exports remain deliberately in the campaign repository. Focused public campaign and state tests pass, an independent Sol High review found no P0/P1/P2 findings, and its low-risk index-3 corruption coverage note was verified as a gap rather than a behavioral defect because exact result verification already validates and decodes that record. The integrated tree passes `npm run validate` with build, lint, typecheck, instance and secret-boundary checks, and the full test suite.

- **TASK-169:** Split the public campaign page into typed feature-owned rendering components. Depends on: none. DoD: `app/page.tsx` retains request-scoped header and identity resolution while feature-owned server components render the published campaign and unavailable states from explicit typed props; current HTML, one `h1`, navigation/actions, published and unavailable behavior, aggregate display, accessibility, and responsive layout remain unchanged, rendered-HTML coverage is current, and build, typecheck, and full validation pass.

  Completion evidence: commits `6c0f86d` and `c3d7b6a` keep `app/page.tsx` to runtime-header and authenticated-view selection, then compose the published and unavailable campaign views from explicit typed feature components under `app/public-campaign/`. Separate header, preview, hero, aggregate, participation, product, process, risks, FAQ, closing, footer, and unavailable components retain the existing HTML/CSS hooks and no dependency or framework was added. Rendered-HTML tests, build, lint, typecheck, and full validation pass. Independent Sol High review found no P0/P1/P2 findings; its residual note is that the existing rendered suite does not separately count `h1` elements or render the preview banner end to end, while direct review confirms the one-heading state model and retained preview semantics.

- **TASK-170:** Extract typed request-capability composition from the application Worker. Depends on: none. DoD: a dedicated Worker composition module returns one typed request-capability result consumed by dispatch and runtime-capability header projection while `createApplicationWorker` retains request normalization, dispatch, image handling, and framework fallback; injected-route precedence, actor/owner isolation, fail-closed unavailable capabilities, advertised-action parity, and existing route behavior remain unchanged, focused cross-role composition and route-dispatch coverage is current, architecture documentation is current, and full validation passes.

  Completion evidence: commit `097bb12` moves request-scoped route dispatcher and renderer-capability projection into `worker/request-capability-composition.ts`, leaving `createApplicationWorker` responsible for normalization, runtime resolution, dispatch invocation, image handling, and framework fallback. Focused Worker tests cover injected dispatch, unavailable owner routes, and owner-render capability isolation. Independent Sol High review found a P2 where a direct caller could provide an injected dispatcher with competing capabilities; commit `326170a` resolves it by returning the injected dispatcher with the all-unavailable projection before inspecting supplied dependencies, and adds a conflicting owner/participant regression. Re-review found no remaining findings. Focused tests, typecheck, lint, and the integrated `npm run validate` pass; architecture documentation now describes the narrow fail-closed composition boundary.

- **TASK-166:** Audit the current TypeScript and React source for parallel-safe refactoring candidates. Depends on: none. DoD: a source-only audit evaluates current modules against the `AGENTS.md` semantic-unit, typed-boundary, React, testability, and shared-hot-spot rules; it records a concise evidence-backed candidate map with exact owned file groups, observable maintenance or testability issue, proposed narrow boundary, and independent validation for each verified candidate; every candidate that merits work is decomposed into a separately numbered, non-overlapping PLAN task with exact direct dependencies and an objective DoD, false positives are explicitly rejected, no application behavior changes, and `npm run agents:check` plus `npm run plan:check` pass.

  Completion evidence: `docs/REFACTORING_AUDIT.md` records the source-only audit, exact owned file groups, observable boundaries, independent validation, risks, and concurrency constraints. Three independent read-only scans covered repository/domain, Worker/http/service, and UI/test-support surfaces; an independent Sol High review confirmed four immediate, non-overlapping follow-ups: TASK-167 through TASK-170. The audit explicitly rejects consolidation of behaviorally distinct local deterministic storage fixtures and a generic mutation-session wrapper that would hide route-specific security policy. It also rejects size-only splits of complex security and persistence modules. TASK-167 and TASK-168 have no semantic dependency but share one factory file and therefore will not receive concurrent write agents; TASK-169 and TASK-170 can proceed concurrently with either repository extraction. No application behavior changed. `npm run agents:check` reports 23,795 bytes under the 32,000-byte limit, and the final plan graph is validated after the replacement tasks are recorded.

- **TASK-165:** Clarify maintainable TypeScript and React feature-boundary rules. Depends on: none. DoD: `AGENTS.md` integrates non-duplicative, repository-accurate guidance for semantic units, typed contracts, dependency direction, extensible behavior, React composition, testability, naming, simplicity, and implementation review; unrelated security, Sites, and planning rules remain intact, no application code changes, and `npm run agents:check` plus `npm run plan:check` pass.

  Completion evidence: `Architecture Rules` now organizes the existing and new guidance around semantic units and feature ownership, typed contracts and extension, React components and testability, trust boundaries, and naming, simplicity, and review signals. The Sites section explicitly keeps UI-framework and dependency additions proportional. A Luna Low mapping audit found the correct integration points and a separate Sol High review found no policy conflict; its three clarity findings on UI dependencies, extension-registration wording, and nearby registration documentation were resolved. `npm run agents:check` reports 23,795 bytes under the 32,000-byte limit, `npm run plan:check` passes, `git diff --check` passes, and the implementation diff contains no application code.

- **TASK-164:** Reconcile the cost-effective subagent execution policy. Depends on: none. DoD: applicable instruction and custom-agent configuration files contain one authoritative, non-contradictory cost-effective subagent-first delegation and GPT-5.6 routing policy using supported model IDs and reasoning-effort syntax; unrelated repository guidance remains unchanged, no application code changes, and `npm run agents:check` plus `npm run plan:check` pass.

  Completion evidence: the repository audit found only `AGENTS.md`; there is no `AGENTS.override.md` or custom-agent configuration file to change. Its obsolete `Multi-agent execution` section was replaced by one authoritative cost-effective subagent-first policy covering main-agent ownership, bounded assignments, worker reporting, model routing, escalation, independent review, and integration. It uses the documented IDs `gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna` plus `reasoning.effort` values `none`, `low`, `medium`, `high`, `xhigh`, and `max`; legacy active routing terms are absent. `npm run agents:check` reports 18,204 bytes under the 32,000-byte limit, `npm run plan:check` passes, `git diff --check` passes, and the implementation diff contains no application code.

- **TASK-163:** Retire fully integrated task branches and stale worktree registrations. Depends on: none. DoD: every registered task worktree and `codex/task-*` branch is compared with `develop`; any branch or checkout containing non-equivalent unintegrated work is retained with a recorded reason, and only missing/prunable worktree registrations plus local or matching remote task branches whose work is fully integrated are removed; the final worktree and branch audit contains no stale registration or eligible completed task branch, `npm run plan:check` passes, and the retained cleanup evidence contains no credential or private value.

  Completion evidence: `git worktree prune --verbose` removed the seven already-missing `/private/tmp/invest-task-*` registrations and no filesystem worktree. The final worktree audit retains only `develop` and the detached acceptance worktree. Every local task branch has non-equivalent work relative to `develop` and is retained: `task-093` (4 unique commits), `task-096` (10), `task-109` (3), `task-110-clean` (2), `task-110` (4), `task-158` (1), `task-159` (2), and `task-160` (4). Matching remote task refs are also retained; no branch was eligible for deletion. The audit output contains only branch names and commit counts, and `npm run plan:check` passes after this archive.

- **TASK-162:** Provide the credential-closed hosted adapter proof runner. Depends on: `TASK-154`. DoD: one operator-only Node command reads exact acceptance issuer, transport, discovery, owner-client, and read-only outsider-client values solely from a same-owner mode-restricted JSON file outside Git, runs the unchanged shared `StorageAdapter` contract through fresh production AittaDB adapters under unique synthetic identifiers, emits only bounded redacted status and cleanup-inventory fields, and fails closed on malformed configuration or credential-bearing errors; deterministic runner, identifier-isolation, output-redaction, documentation, focused-test, and full-validation checks pass.

  Acceptance evidence: commits `bff5902`, `41c5327`, `b7b9487`, `856ef5f`, and `fc4173b` add the operator-only command, closed external configuration loader, immutable runtime snapshot, unique namespaced fixture projection, production adapter and service-token composition, and bounded fixed report. The loader rejects symlinks, hard links, permission or ownership drift, duplicate JSON members, pairwise credential reuse, every Git repository or worktree, and malformed or production-shaped targets. Before any token request or adapter construction, the actual configured transport must return an exact fresh challenge-, URL-, issuer-, environment-, media-, and no-store-bound disposable-proof assertion; crossed, cached, denying, malformed, redirected, oversized, stalled, or unavailable responses fail closed and rejected bodies are cancelled. The exported runner has no execution dependency hooks. Focused tests run the unchanged shared contract through isolated names and the real `AittaDBStorageAdapter` plus service-token provider, verify exact owner and outsider scopes, immutable configuration under concurrent mutation, assertion denial before credentials, output redaction, and response cancellation; all 9 passed. Independent Sol High review iteratively found and confirmed resolution of production-target, configuration-file aliasing, default-composition coverage, dynamic-error disclosure, durability-overclaim, mutable-input, server-assertion, injected-hook, and response-cancellation findings; final review reported no P0, P1, or P2 findings. Final `npm run validate` passed agent-size, plan, instance-boundary, lint, typecheck, production build, runtime-secret checks across 378 release files, and all 1,268 tests. Residual uncertainty: the disposable server assertion and atomic transaction action are not yet live, so credential-bound hosted behavior, restart durability, namespace grants, and cleanup remain `TASK-131`, `TASK-113`, and `TASK-132` work.

- **TASK-111:** Prove source and artifact credential non-exposure. Depends on: `TASK-093`, `TASK-094`, `TASK-096`, `TASK-074`, `TASK-104`, `TASK-076`, `TASK-106`, `TASK-133`, `TASK-134`. DoD: repository, generated Sites archive, built Worker, source maps, manifests, migrations, HTML, JSON, downloads, redirects, CSP, and fixed errors contain no instance credential, bearer token, client secret, production identity, or private environment value; adversarial sentinel scans and focused boundary tests plus full validation pass.

  Acceptance evidence: commits `b25a4c6`, `bb371d4`, `3708213`, and `5d2227b` establish the tracked-source, executable-input, built-tree, response, and exact packaged-archive disclosure boundaries. `npm run sites:package` now requires an exact duplicate-free three-field hosting manifest, creates a mode-`0600` archive with normalized `root/root` and numeric `0/0` metadata, and scans all 115 archive entries plus compressed headers, names, metadata, regular contents, and 473 release files through non-echoing diagnostics. Adversarial tests cover external same-owner sentinels, malformed sentinel input, source maps, manifests, migrations, HTML, hypermedia JSON, downloads, redirects, CSP, fixed errors, gzip filenames, unsafe and credential-bearing entry names, duplicate manifest members, and exact public, participant, owner, success, failure, and private-download runtime contracts. Owner authorization values no longer enter owner-home HTML or hypermedia. Independent Sol High review found and then confirmed resolution of archive-creation, metadata, manifest, owner-identity, runtime-matrix, coverage, and sentinel-file findings; final re-review reported no P0, P1, or P2 findings. Final `npm run validate` passed agent-size, plan, instance-boundary, lint, typecheck, production build, runtime-secret checks, and all 1,259 tests. Residual uncertainty: live browser, network, Worker-log, and hosted storage behavior remain `TASK-112` and `TASK-131` acceptance work.

- **TASK-096:** Compose the hosted investment-interest workflow. Depends on: `TASK-073`, `TASK-090`, `TASK-095`, `TASK-097`, `TASK-154`, `TASK-158`. DoD: a permitted investor participant reaches the existing HTML and hypermedia lifecycle through the persistent participant reader, atomic mutation port, current campaign and package acknowledgment, and hosted mutation session; create, edit, withdraw, reactivate, closed-phase, stale-policy, proof, retry, restart, owner, foreign, and non-disclosure hosted-composition tests plus full validation pass.

  Acceptance evidence: commits `24ba5e7` through `ff0cbe6` compose the subject-bound persistent investment reader and atomic mutation port with current campaign, participant, package, acknowledgment, and hosted proof policy. HTML and versioned hypermedia now cover create, edit, withdrawal, reactivation, policy closure and movement, exact retry, response loss, Worker reconstruction, owner and foreign denial, and non-disclosing failures. Compact collection summaries authenticate complete revision-one creation evidence within an exact bounded read budget. Terminal withdrawal recovery has its own HTML/JSON resource and mandatory actor-, route-, method-, indication-, command-, and revision-bound exact proof; maximum Unicode subject, origin, indication, operation, and revision values remain inside the bounded encrypted cookie contract. Independent Sol High review found no remaining P0, P1, or P2 issue. Final `npm run validate` passed agent-size, plan, instance-boundary, lint, typecheck, production build, runtime-secret checks, and all 1,252 tests. Residual uncertainty: live credential-bound AittaDB behavior and the deployed public-aggregate journey remain `TASK-131` and `TASK-139` acceptance work.

- **TASK-107:** Wire the audited owner CSV review export. Depends on: `TASK-068`, `TASK-069`, `TASK-089`, `TASK-073`, `TASK-076`, `TASK-105`, `TASK-133`, `TASK-134`. DoD: one owner-only route streams bounded current AittaDB-backed campaign, package, participant, founder, indication, aggregate, and audit rows as formula-neutralized CSV with private download headers and immutable closed export audit evidence; anonymous, non-owner, stale, oversized, looping-page, cancellation, changed retry, restart, and partial-download paths remain credential-free and fail closed, and full validation passes.

  Planning evidence: Deferred unchanged to `BACKLOG-007` so audited CSV export composition does not gate the first functional MVP. This is a scope move, not completion evidence or a capability claim.

- **TASK-108:** Wire the audited owner JSON backup export. Depends on: `TASK-068`, `TASK-069`, `TASK-089`, `TASK-073`, `TASK-076`, `TASK-105`, `TASK-133`, `TASK-134`. DoD: one owner-only route streams a versioned bounded current-state JSON backup from the AittaDB-backed campaign, package, participant, founder, indication, aggregate, and audit projections with private download headers and immutable closed export audit evidence; anonymous, non-owner, stale, oversized, looping-page, cancellation, changed retry, restart, and partial-download paths remain credential-free and fail closed, and full validation passes.

  Planning evidence: Deferred unchanged to `BACKLOG-008` so audited JSON backup composition does not gate the first functional MVP. This is a scope move, not completion evidence or a capability claim.

- **TASK-145:** Prove the hosted CSV review export. Depends on: `TASK-107`, `TASK-112`. DoD: the configured acceptance owner downloads one bounded formula-neutralized CSV containing only the expected namespaced current review projection with private cache/download headers and one immutable export event; anonymous, non-owner, stale, oversized, looping-page, cancellation, retry, restart, and partial-download paths fail closed.

  Planning evidence: Deferred unchanged to `BACKLOG-009` with its export prerequisite mapped to `BACKLOG-007`. Hosted CSV proof is no longer an MVP acceptance or production-publication prerequisite.

- **TASK-146:** Prove the hosted JSON backup export. Depends on: `TASK-108`, `TASK-112`. DoD: the configured acceptance owner downloads one bounded versioned JSON backup containing only the expected namespaced current-state allowlist with private cache/download headers and one immutable export event; anonymous, non-owner, stale, oversized, looping-page, cancellation, retry, restart, and partial-download paths fail closed.

  Planning evidence: Deferred unchanged to `BACKLOG-010` with its export prerequisite mapped to `BACKLOG-008`. Hosted backup proof is no longer an MVP acceptance or production-publication prerequisite.

- **TASK-159:** Provide the legacy investment-ownership migration command. Depends on: `TASK-073`, `TASK-154`, `TASK-155`. DoD: one backend-only credential-closed operator command consumes an explicitly authoritative bounded subject inventory and initializes each legacy participant's complete ownership root, index, and witness without a browser route or ordinary-request absence fallback; empty, active, withdrawn, rejected, maximum-record, over-capacity, incomplete, crossed, corrupt, exact-retry, resume, restart, redaction, documentation, and full-validation tests pass.

  Planning evidence: Deferred unchanged to conditional `BACKLOG-011`; a fresh MVP namespace consumes no legacy ownership state. Existing task-branch work remains isolated and is not completion evidence or part of this planning commit.

- **TASK-160:** Provide the legacy indication-summary migration command. Depends on: `TASK-073`, `TASK-097`, `TASK-154`. DoD: one backend-only credential-closed operator command consumes an explicitly authoritative bounded subject-indication inventory, fully verifies every schema-4 indication's immutable ancestry, fields, lifecycle, and active lease, and compare-and-sets its current head to the schema-5 bounded summary envelope without changing the domain revision; empty, maximum-history, missing, crossed, corrupt, concurrent, exact-retry, response-loss, resume, restart, redaction, no-browser-GET-write, documentation, and full-validation tests pass.

  Planning evidence: Deferred unchanged to conditional `BACKLOG-012`; a fresh MVP namespace consumes no schema-4 indication state. Existing task-branch work remains isolated and is not completion evidence or part of this planning commit.

- **TASK-136:** Prove persistent audit-producer integration. Depends on: `TASK-068`, `TASK-069`, `TASK-073`, `TASK-076`, `TASK-089`, `TASK-103`, `TASK-106`, `TASK-107`, `TASK-108`, `TASK-109`. DoD: campaign, package, participant, indication, aggregate, moderation, reconciliation, notification, export, and deletion producers append their canonical closed events exactly once across exact retry, changed retry, rollback, concurrency, and Worker reconstruction; unknown or private evidence remains non-disclosing, focused cross-producer tests pass, and full validation passes.

  Planning evidence: Deferred unchanged to `BACKLOG-013`; its broad optional-feature producer matrix no longer gates the core campaign, founder, and investment MVP journeys. Existing per-feature audit guarantees remain governed by their completed task evidence.

- **TASK-144:** Prove hosted audit and notification activity. Depends on: `TASK-105`, `TASK-106`, `TASK-112`, `TASK-136`. DoD: the configured acceptance owner pages canonical audit events and private notification history, records copied and sent facts separately, and observes each expected producer event once across retries and reconstruction through equivalent bounded HTML and hypermedia; anonymous, non-owner, malformed, unknown, and private-detail paths disclose nothing.

  Planning evidence: Deferred unchanged to `BACKLOG-014`; broad hosted audit and notification activity is no longer an MVP acceptance or publication prerequisite.

- **TASK-142:** Prove the hosted indication-moderation journey. Depends on: `TASK-104`, `TASK-112`. DoD: the configured acceptance owner pages and inspects opaque indication resources and rejects one active synthetic indication exactly once through equivalent bounded HTML and hypermedia; aggregate, participant-visible reason, audit, and notification effects remain consistent across stale, retry, non-owner, foreign, and restart paths.

  Planning evidence: Deferred unchanged to `BACKLOG-015`; hosted moderation proof is not required to collect and review non-binding MVP interest.

- **TASK-143:** Prove the hosted aggregate-reconciliation journey. Depends on: `TASK-076`, `TASK-112`. DoD: the configured acceptance owner compares calculated and stored synthetic aggregate state and applies only an exact preview-bound correction through equivalent bounded HTML and hypermedia; matching, stale, retry, non-owner, overflow, failure, and restart paths remain safe and non-disclosing.

  Planning evidence: Deferred unchanged to `BACKLOG-016`; hosted reconciliation proof is not required for the first functional MVP acceptance matrix.

- **TASK-114:** Prove acceptance service-client secret rotation. Depends on: `TASK-113`. DoD: rotating the acceptance service-client secret makes the old credential unusable, lets the exact acceptance Worker continue through the replacement hosted secret, preserves stored synthetic records, and changes no source, client scopes, origins, redirects, access mode, unrelated AittaDB setting, or production environment; retained evidence contains no old or new credential.

  Planning evidence: Deferred unchanged to `BACKLOG-017`; least-privilege authorization remains active MVP work, while the operational rotation rehearsal no longer gates initial publication.

- **TASK-099:** Prove hosted acceptance issuer reachability. Depends on: `TASK-098`. DoD: the exact acceptance Worker version reaches and validates the configured issuer's discovery contract without weakening issuer, endpoint, redirect, status, media, size, encoding, JSON, grant, response-mode, PKCE, or confidential-authentication checks; retained private evidence contains only the fixed failure phase or success, Worker logs and public output remain credential-free, and the deployed source version plus rollback version are recorded without secrets.

  Planning evidence: Deferred unchanged to `BACKLOG-018`; the disposable owner-initiated OAuth proof path is separate from the production service-client and participant identity boundaries used by the MVP.

- **TASK-101:** Prove the hosted OAuth callback token path. Depends on: `TASK-099`, `TASK-100`. DoD: an owner-initiated acceptance flow reaches the configured provider, returns through the exact callback, exchanges one S256 authorization code confidentially, validates introspection audience, client, scope, time, subject, and replay state, and records only closed proof evidence; failed and successful paths expose no code, token, secret, identity, provider body, or exception, and focused hosted verification plus full local validation pass.

  Planning evidence: Deferred unchanged to `BACKLOG-019`; its issuer prerequisite is mapped to `BACKLOG-018`, and the disposable callback proof no longer gates the MVP runtime.

- **TASK-127:** Revoke the disposable OAuth proof client. Depends on: `TASK-101`. DoD: all proof-client grants are revoked and the disposable client is disabled or removed from the acceptance AittaDB deployment without changing another client, namespace, data record, feature flag, access mode, or production setting; retained evidence contains no client identifier, identity, credential, token, or private environment value.

  Planning evidence: Deferred unchanged to `BACKLOG-020`. Deferral is not authorization to retain a credential indefinitely; the disposable proof boundary must be reviewed before the proof path is resumed or reused.

- **TASK-128:** Remove disposable OAuth configuration from Investor acceptance. Depends on: `TASK-101`. DoD: the proof-client secret and every now-unused interactive OAuth setting are removed from the Investor acceptance Sites environment without changing source, the application service client, D1/R2 bindings, access mode, unrelated environment values, or production; the owner connection becomes unavailable and retained evidence contains no removed value.

  Planning evidence: Deferred unchanged to `BACKLOG-021`. Deferral is not authorization to expose or reuse the hosted secret; the disposable proof boundary must be reviewed before the proof path is resumed.

- **TASK-129:** Remove temporary AittaDB acceptance administration. Depends on: `TASK-101`. DoD: the AittaDB project removes the temporary Investor proof administrator subject from the acceptance deployment without changing another administrator, client, namespace, feature flag, access mode, data record, or production environment; anonymous and former-admin access to client registration fail identically and no identity or environment value is retained.

  Planning evidence: Deferred unchanged to `BACKLOG-022`. Deferral is not authorization to broaden administration; the temporary acceptance boundary must be reviewed before the proof path is resumed.

- **TASK-130:** Verify disposable OAuth cleanup boundaries. Depends on: `TASK-127`, `TASK-128`, `TASK-129`. DoD: former proof initiation, callback, client use, hosted-secret access, and administrator access all fail as expected while the application service client, D1/R2, accepted application workflows, and unrelated acceptance settings remain healthy; repositories are clean and retained evidence contains no identity, credential, token, client identifier, or private environment value.

  Planning evidence: Deferred unchanged to `BACKLOG-023` with its three cleanup prerequisites mapped to `BACKLOG-020` through `BACKLOG-022`.

- **TASK-110:** Compose the hosted account-deletion request flow. Depends on: `TASK-090`, `TASK-092`, `TASK-109`. DoD: the existing profile deletion action invokes the atomic coordinator through the hosted mutation session, immediately removes participant/package/founder/investor capabilities according to policy, preserves only the independently allowed marketing-withdrawal action, and passes equivalent HTML/JSON, origin, proof, retry, stale, restart, owner, foreign, and non-disclosure tests plus full validation.

  Planning evidence: Deferred unchanged to `BACKLOG-024`; automated hosted deletion is no longer an MVP prerequisite. Reviewed but unfinished branch work remains isolated from `develop`, and the review findings are not represented as resolved or completed here.

- **TASK-147:** Prove the hosted account-deletion journey. Depends on: `TASK-110`, `TASK-112`. DoD: one acceptance participant requests deletion and immediately loses participant, package, founder, and investor capabilities according to policy while every active interest and aggregate contribution is withdrawn exactly once and independently allowed marketing withdrawal remains available; equivalent HTML/hypermedia, retry, stale, restart, owner, and foreign paths remain safe and non-disclosing.

  Planning evidence: Deferred unchanged to `BACKLOG-025` with its implementation prerequisite mapped to `BACKLOG-024`; hosted automated-deletion proof no longer gates MVP acceptance or publication.

- **TASK-109:** Compose the atomic participant account-deletion coordinator. Depends on: `TASK-064`, `TASK-067`, `TASK-089`, `TASK-071`, `TASK-073`, `TASK-105`, `TASK-155`, `TASK-156`, `TASK-157`. DoD: one retry-stable AittaDB transaction marks the participant deletion requested, withdraws every active founder application and investment indication, updates aggregate contributions, and appends closed audit evidence or changes none; marketing consent remains independent, and empty, partial-failure, changed-retry, stale, concurrency, maximum-record, malformed-result, restart, and foreign-access tests plus full validation pass.

  Acceptance evidence: `StorageParticipantAccountDeletionRepository` now binds the trusted participant, exact profile revision, active founder withdrawal or founder-absence assertion, complete bounded investment ownership set, every active withdrawal and aggregate contribution, one aggregate update, closed audit evidence, and an immutable retry receipt into one transaction or changes none. Empty and inactive accounts still commit founder, index, and witness barriers; cleanup derives historical amount validation from the aggregate's persisted immutable currency; and the audit identity binds the complete receipt-effect fingerprint so omitted withdrawals or altered aggregate results fail closed. Marketing withdrawal remains independent. Maximum founder history plus 100 owned indications, four active withdrawals, a 25-mutation commit, response loss, and replay stay inside the exported combined read ceiling. Direct founder creation, indication creation, and reactivation races roll back every deletion effect. Changed retry, stale input, partial failure, malformed evidence, corrupt effects, configuration evolution, restart, anonymous, foreign, and non-disclosure paths pass. Clean commits `e3454f8`, `10899eb`, `732be04`, and `73cfc7b` exclude the private specification from both task diff and task history. Independent Sol High review found and then confirmed resolution of founder-absence, receipt-integrity, and read-proof gaps; final re-review reported no findings. The exact integrated tree passed lint, typecheck, production build, plan and instance checks, runtime-secret scanning, and all 1,236 tests. Residual uncertainty: hosted profile composition remains `TASK-110`, complete persistent producer proof remains `TASK-136`, and live AittaDB atomic behavior remains a separate acceptance gate.

- **TASK-158:** Provision empty investment ownership with first registration. Depends on: `TASK-089`, `TASK-093`, `TASK-155`. DoD: the first persistent participant registration atomically creates the subject's empty immutable investment-ownership root, index, and completeness witness with the profile or creates none; exact and delayed registration retries remain stable after later indication activity, while existing-profile, conflict, rollback, restart, owner, foreign, malformed-result, and non-disclosure tests plus full validation pass.

  Acceptance evidence: first persistent registration now commits the current profile, immutable profile revision, immutable empty ownership root, empty index, and empty completeness witness in one bounded five-record transaction or creates none. Exact and stale-policy recovery authenticate the original ownership operation and the complete current bounded ownership state, so later valid indication activity preserves the registration replay while missing, partial, crossed, or corrupt ownership fails closed. Existing legacy profiles never gain ownership through ordinary absence fallback. Concurrent registration, changed retry, adapter failure, malformed or lost transaction evidence, restart, owner and foreign access, maximum wire size, redaction, and distinct malformed-subject collision cases pass. The clean rebased commits `95fe136`, `8be4c7f`, and `b166ced` exclude the private specification from both diff and history. Independent review reported no findings and passed 246 focused tests; the exact integrated branch passed lint, typecheck, production build, plan and instance checks, the runtime-secret scan, and all 1,204 tests. Residual uncertainty: legacy participants still require `TASK-159`, and live AittaDB atomic behavior remains hosted acceptance work.

- **TASK-157:** Prepare the bounded account-deletion investment withdrawal set. Depends on: `TASK-073`, `TASK-155`, `TASK-156`. DoD: one subject-bound primitive materializes the bounded owned indication index, stages deterministic withdrawal current/history and active-lease changes for every active indication, replaces every affected aggregate contribution, and calculates one exact aggregate snapshot update without mutating storage itself; empty, withdrawn, rejected, four-active maximum, over-capacity, stale, missing, corrupt, overflow, restart, finite-read, documentation, and full-validation tests pass.

  Acceptance evidence: the subject-bound primitive now authenticates the complete bounded ownership proof, reconstructs only the at-most-four active indications, and stages deterministic current, history, active-lease, index, witness, aggregate-contribution, and single aggregate-snapshot mutations behind the shared request-local transaction without committing storage. Empty and inactive sets stage two index-and-witness revision barriers, so coordinated and lower-level create or reactivation races conflict atomically; an all-selected aggregate must also exhaust its stored total. Maximum work remains 19 unique mutations with finite exported reads. Missing, crossed, stale, corrupt, over-capacity, overflow, response-loss, restart, and malformed evidence fail closed without partial work. Independent review findings on the empty race, aggregate invariant, and direct witness-CAS coverage were resolved in `dc76e70` and `e22b447`; final re-review reported no findings. The exact integrated tree passed lint, typecheck, production build, plan and instance checks, the runtime-secret scan, and all 1,190 tests. Residual uncertainty: atomic profile/founder/audit composition remains `TASK-109`, and live AittaDB transaction behavior remains hosted acceptance work.

- **TASK-161:** Document the multi-agent execution policy. Depends on: none. DoD: `AGENTS.md` contains one compact section assigning the requested primary-agent ownership, model-routing boundaries, independent-write constraint, complete delegated-unit contract, reporting requirements, and final integration duties without changing unrelated guidance; the instruction-size and plan checks pass.

  Acceptance evidence: `AGENTS.md` now contains one compact `Multi-agent execution` section assigning GPT-5.6 Sol Ultra primary ownership, the requested Luna, Terra, and Sol review routing, delegation exclusions, independent-write boundaries, complete implementation-unit and reporting requirements, and primary-agent integration and validation duties. Existing instructions are unchanged outside the appended section. `npm run agents:check` reports 13,893 bytes under the 32,000-byte limit, `npm run plan:check` validates the 47-task graph before archival, and commit `ae014c5` records the requested documentation checkpoint. Residual uncertainty: model availability remains host-dependent as the policy states.

- **TASK-134:** Wire the owner founder-review detail resource. Depends on: `TASK-071`. DoD: the configured owner can inspect one opaque founder application and its bounded history from persistent records while subjects stay out of URLs, logs, and public output; anonymous, non-owner, malformed, missing, foreign, corruption, and restart paths remain finite and non-disclosing, equivalent HTML/hypermedia detail tests pass, and full validation passes.

  Acceptance evidence: the configured owner can now resolve one stable opaque founder-review identifier to one persistent application and reconstruct its bounded immutable lifecycle without putting participant subjects or storage-key digests in URLs, logs, HTML, hypermedia, or fixed failures. Missing legacy review identities use a bounded private lookup and backfill contract, while direct current identities remain restart-stable. Equivalent HTML and versioned hypermedia preserve application fields, history, professional-profile links, navigation, and authorization; links are escaped, HTTPS-only, no-referrer protected, and omitted when unavailable. Anonymous, non-owner, malformed, missing, crossed, foreign, maximum revision and five-chunk payload, corruption, owner rotation, restart, and non-disclosure paths pass without collection listing for current records. Fresh independent no-edit review of `a63bee0` found no defect and passed 61 focused plus 1,172 full tests. The no-conflict rebase onto TASK-104 preserved patch identity or only additive surrounding integration context at `83797e0`; 36 cross-capability tests and full `npm run validate` passed all 1,176 tests, and the exact fast-forwarded `develop` tree passed the same 1,176-test gate before archival. Residual uncertainty: live AittaDB behavior and the bounded legacy backfill remain hosted acceptance evidence, while browser accessibility and visual parity remain `TASK-150`.

- **TASK-104:** Compose the hosted owner indication-moderation resources. Depends on: `TASK-102`, `TASK-103`, `TASK-135`. DoD: the configured owner reaches equivalent bounded HTML and hypermedia collection/detail resources and can invoke only the currently advertised rejection action through the hosted mutation session; anonymous, non-owner, missing, foreign, stale, malformed, oversized, origin, proof, retry, rollback, restart, and non-disclosure route tests plus full validation pass.

  Acceptance evidence: the configured owner now reaches bounded opaque indication collection and detail resources through equivalent HTML and versioned hypermedia, with rejection advertised and accepted only for an eligible active detail through the actor- and origin-bound hosted mutation session. Hosted composition uses the persistent collection, detail, and atomic rejection capabilities without exposing subjects, storage keys, credentials, private notes, or backend causes. Anonymous, non-owner, owner rotation, missing, crossed, stale, malformed, oversized, unsupported-media, proof, replay, response-loss, rollback, restart, and non-disclosure paths pass. A fresh no-edit review found no remaining defect before rebase; the implementation commit remained patch-identical and the proof commit gained only the independently required TASK-155 ownership fixture. The rebased tip `b1aa71f` passed 133 focused cross-capability tests and full validation with 1,165 tests, and the exact fast-forwarded `develop` tree passed `npm run validate` again with all 1,165 tests before archival. Residual uncertainty: hosted acceptance remains `TASK-142`, while live AittaDB atomic behavior remains separately gated.

- **TASK-155:** Enforce the deletion-safe active-indication capacity. Depends on: `TASK-073`. DoD: the persistent participant investment repository permits at most four active owned indications across create and reactivation so the accepted 25-mutation transaction contract can always withdraw the maximum set atomically; withdrawn and rejected history consumes no active slot, concurrent exact retry cannot admit a fifth active indication, pre-existing over-capacity or corrupt state fails closed without mutation, and focused limit, concurrency, restart, documentation, and full-validation tests pass.

  Acceptance evidence: the persistent participant investment repository now authenticates one explicit ownership root, bounded index, completeness witness, every compact current/terminal/field commitment, and each exact active uniqueness lease before admitting create or reactivation. At most four active owned indications can pass; withdrawn and rejected history consumes no slot, a competing activation cannot admit a fifth, and missing, crossed, corrupt, moving, over-capacity, response-loss, changed-retry, concurrent-initialization, restart, and foreign evidence fails closed without collection scans or partial mutation. The maximum ownership proof stays within the documented 240-read ceiling. Independent no-edit review of `12859cd` found no correctness, security, disclosure, dependency, or test issue and reran 55 focused tests plus full validation successfully. The six commits rebased patch-identically as `f12dc9c` through `8e8d2cf`; post-rebase focused validation passed all 85 selected repository/detail/rejection tests and full `npm run validate` passed all 1,161 tests. Residual uncertainty: first-registration provisioning and legacy ownership migration remain the separate `TASK-158` and `TASK-159` prerequisites before ordinary hosted investment access can rely on this ownership proof.

- **TASK-076:** Wire owner aggregate reconciliation. Depends on: `TASK-067`, `TASK-073`, `TASK-154`. DoD: the configured owner can compare calculated and stored aggregate state and apply only an exact preview-bound correction through AittaDB-backed atomic aggregate/audit persistence; matching, stale, overflow, retry, failure, non-owner, and restart paths remain safe and non-disclosing; HTML/hypermedia parity tests and full validation pass.

  Acceptance evidence: the configured owner can now compare a bounded, fully verified calculated aggregate against the stored private aggregate through equivalent HTML and versioned hypermedia resources, then apply only the exact preview-bound correction with one atomic aggregate-and-audit transaction. Exact replay refreshes the current reconciliation state before projecting actions or proof, and correction authorization is bound to the complete canonical campaign revision rather than a partial identifier. Matching, stale, changed retry, overflow, concurrent correction, transaction failure, malformed evidence, response loss, owner rotation, anonymous, non-owner, restart, and non-disclosure paths pass. Independent no-edit review found no remaining issue before the mechanical rebase; commits `c76d5b1` through `e42065e` preserve both reconciliation and the already integrated notification-history capability. The exact rebased branch passed 135 focused cross-capability tests, `git diff --check`, and full `npm run validate` with all 1,133 tests. Residual uncertainty: live AittaDB atomic transaction behavior remains hosted acceptance work, and production correction remains unavailable until deployment readiness gates pass.

- **TASK-106:** Compose the persistent owner notification-history resource. Depends on: `TASK-103`, `TASK-105`. DoD: a bounded AittaDB notification repository and owner-authorized HTML/hypermedia route page private manual templates and append retry-stable copied and owner-entered sent facts as separate audited actions; templates never enter URLs, public errors, logs, or unrelated audit evidence, and malformed, oversized, stale, retry, rollback, restart, anonymous, and non-owner tests plus full validation pass.

  Acceptance evidence: the configured owner now pages bounded private manual-notification history through equivalent HTML and versioned hypermedia resources, then records copied and owner-entered sent activity as distinct audited, retry-stable mutations. Collection items are accepted only when each current snapshot is canonically equal to its exact immutable terminal revision; missing, malformed, crossed, duplicate, or current-only divergent history fails closed within one list plus at most 25 direct reads. Owner authorization precedes storage, and templates remain absent from URLs, public errors, logs, and unrelated audit evidence. Retry, response-loss, rollback, restart, cursor, maximum-page, malformed, stale, anonymous, and non-owner coverage passes. Commits `ae02b9d` through `f0d7167` contain the implementation and history-authentication hardening. The exact branch passed full `npm run validate` with all 1,087 tests; independent no-edit review of `f0d7167` found no remaining issue and reran 28 focused tests, typecheck, lint, and diff checks successfully. Residual uncertainty: complete producer coverage remains `TASK-136`, and live AittaDB paging and mutation behavior remain hosted acceptance work.

- **TASK-156:** Add the bounded staged-storage transaction primitive. Depends on: `TASK-154`. DoD: one framework-independent `StorageAdapter` staging boundary overlays exact reads and staged puts/deletes/checks under one operation ID, performs no durable mutation before its single commit, rejects duplicate keys, changed revisions, oversized or over-25 mutation requests, and malformed or reordered results, preserves exact replay evidence, and passes deterministic partial-failure, rollback, limit, documentation, and full-validation tests.

  Acceptance evidence: one framework-independent staged transaction now overlays exact reads plus staged puts, deletes, and revision/absence checks without durable mutation before one sealed commit. It serializes lifecycle changes, rejects duplicate keys, pending or late work, unsafe revisions, over-25 mutations, and byte-limit overflow before commit, coalesces concurrent commits, and retains one immutable normalized intent after a lost or failed response so only an exact retry can continue. Commit evidence requires a closed dense envelope with the exact ordered keys, revisions, values, null positions, and boolean replay marker; malformed, reordered, accessor-backed, and hostile adapter results fail with allowlisted codes and no leaked cause. Deterministic tests cover overlay reads, partial staging failure, rollback, response loss, changed retry, concurrent commit, exact limits, malformed evidence, throwing accessors, and failure sanitization. Independent re-review found no remaining defect at `eb3e125`; rebased commits `ba38d4a` and `4c24a0b` preserve the reviewed implementation and hardening. Integrated `npm run validate` passes lint, typecheck, production build, all 1069 tests, the 12,322-byte `AGENTS.md` limit, a valid 49-task graph before archival, clean Sites instance boundaries, and a clean 232-file runtime-secret scan. Residual uncertainty: TASK-157 and TASK-109 remain the bounded deletion-set and coordinator integrations, and live AittaDB behavior remains hosted acceptance work.

- **TASK-094:** Compose the hosted founder application route. Depends on: `TASK-071`, `TASK-090`, `TASK-154`. DoD: a permitted founder participant reaches the existing HTML and hypermedia application contract through the persistent subject-bound repository, current configured contribution choices, and hosted mutation session; closed-phase, malformed, oversized, stale, proof, retry, restart, owner, foreign, and non-disclosure route and hosted-composition tests plus full validation pass.

  Acceptance evidence: a permitted registered founder now reaches the persistent subject-bound application lifecycle through equivalent native HTML and versioned hypermedia resources, current configured contribution choices, and the hosted actor/origin-bound mutation session. Create and edit atomically bind the sampled campaign and participant eligibility revisions, terminal withdrawal remains exactly replayable after response loss and Worker reconstruction, and existing applications stay readable or withdrawable after phase and interest changes. Anonymous, owner, inactive, foreign, stale-policy, malformed, oversized, unsupported-method, one-use proof, changed retry, maximum-history, and storage-failure paths remain finite and non-disclosing. The operation-ID ceiling is consistently 128 characters across the shared parser, action contract, body budget, service, and terminal request scope; both HTML and JSON reject 129 characters before mutation. Independent re-review found no remaining defect at `9489ecd`, and the rebased integration tip `23345a6` preserves the newer owner review capabilities alongside the founder route. Integrated `npm run validate` passes lint, typecheck, production build, all 1051 tests, the 12,322-byte `AGENTS.md` limit, a valid 50-task graph before archival, clean Sites instance boundaries, and a clean 231-file runtime-secret scan. Residual uncertainty: the hosted acceptance founder journey remains `TASK-138` and live transport behavior remains bounded acceptance work.

- **TASK-100:** Configure the disposable acceptance OAuth client. Depends on: `TASK-051`. DoD: the selected acceptance AittaDB deployment registers one confidential Investor App Authorization Code client with S256 PKCE, only the required storage scopes, and only exact acceptance callback URIs; its secret exists solely in the acceptance Sites environment, no value enters Git or retained evidence, and discovery plus client metadata are verified without performing a participant workflow.

  Acceptance evidence: the disposable acceptance AittaDB deployment advertises Authorization Code, S256 PKCE, confidential-client authentication, introspection, and the required storage scopes. Its administrator resource showed one active confidential Investor App proof client with only the exact configured acceptance callback, only the read-only `storage.read` proof scope, and no browser origin. The matching acceptance Sites environment contains the client configuration and an unreadable hosted secret; no identifier, secret, identity, token, or private environment value was copied into Git or retained evidence. Anonymous client administration remained unauthorized, no participant workflow was performed, and neither the production AittaDB deployment nor production Investor App settings were changed. Residual uncertainty: issuer reachability and the complete callback/token/introspection path remain independently bounded by `TASK-099` and `TASK-101`; disposable cleanup remains `TASK-127` through `TASK-130`.

- **TASK-133:** Wire the owner founder-review collection. Depends on: `TASK-071`. DoD: the configured owner can page a bounded opaque founder-review collection backed by persistent applications while subjects stay out of URLs, cursors, logs, and public output; anonymous, non-owner, malformed cursor, oversized page, corruption, and restart paths remain finite and non-disclosing, equivalent HTML/hypermedia collection tests pass, and full validation passes.

  Acceptance evidence: the configured owner now reaches one private, non-cacheable founder-review collection backed by persistent current applications. Native HTML and versioned hypermedia expose the same bounded summary allowlist and opaque one-way review identifiers, with page size capped at 25 and complete current-field verification constrained to an observed 300-record-read ceiling. Anonymous and authenticated non-owner callers stop before storage; malformed and oversized cursors, looping or malformed backend pages, edited or withdrawn current-record drift, corrupt field ancestry, restart continuation, and repository failures remain finite and non-disclosing. The repository factory exposes only a frozen singleton `list` port with no detail, mutation, generic storage, or logging surface, and hosted runtime composition installs the owner route without coupling it to indication moderation. Independent review findings were resolved before integration. Commits `dfdcd11` and `646931f` contain the rebased implementation and summary-verification hardening. Integrated `npm run validate` passes lint, typecheck, production build, all 1035 tests, the 12,322-byte `AGENTS.md` limit, a valid 52-task graph before archival, clean Sites instance boundaries, and a clean 231-file runtime-secret scan. Residual uncertainty: one-record founder detail remains `TASK-134`, and live hosted owner review remains acceptance work.

- **TASK-103:** Compose atomic owner indication rejection. Depends on: `TASK-067`, `TASK-073`, `TASK-097`. DoD: one owner-bound retry-stable AittaDB transaction rejects an active indication with a bounded participant-visible reason, removes its aggregate contribution, updates the aggregate, appends closed owner audit evidence, and creates one bounded manual-notification template or changes none; stale, changed retry, concurrency, malformed result, rollback, restart, and foreign-access repository tests plus full validation pass.

  Acceptance evidence: one configured-owner write capability now binds an authenticated opaque review resource to its trusted internal indication and commits the terminal rejected revision, active-contribution removal, exact aggregate snapshot, closed audit event, bounded participant-visible manual-notification template, and immutable retry receipt in one transaction. Exact and concurrent retries return the original server-timed result across repository reconstruction and commit-response loss, while changed operation reuse, stale revisions, crossed review identities, malformed requests, foreign owners, corrupt persisted effects, and malformed transaction evidence fail closed without another write. Injected transaction failure rolls back every effect and receipt; the outer request and recovery reads remain explicitly bounded. The rebase preserves the independently integrated collection and detail contracts while keeping rejection as a narrow atomic write lane for hosted composition. Independent review findings were resolved before integration. Commits `bd564a9` and `0d73e42` contain the rebased implementation and replay hardening. Integrated `npm run validate` passes lint, typecheck, production build, all 1026 tests, the 12,322-byte `AGENTS.md` limit, a valid 53-task graph before archival, clean Sites instance boundaries, and a clean 231-file runtime-secret scan. Residual uncertainty: the route-level owner moderation composition and persistent notification-history resource remain `TASK-104` and `TASK-106`.

- **TASK-135:** Persist the owner indication-review detail projection. Depends on: `TASK-073`, `TASK-097`. DoD: one owner-bound AittaDB repository resolves one opaque private indication detail and bounded history without placing a subject, company identifier, or note in URLs, logs, or public failures; anonymous, non-owner, malformed, missing, crossed, corruption, restart, and finite-read repository tests plus full validation pass.

  Acceptance evidence: one configured-owner repository now resolves an opaque authenticated review identifier directly to a single persistent indication and reconstructs its bounded immutable lifecycle without listing participant records or exposing subjects in resource identities. Rejected details also resolve only the current bounded manual-notification projection, with actor continuity checked across owner rotation and copy/sent history. Malformed, tampered, missing, crossed, foreign, rewritten, oversized, accessor-backed, and storage-failure paths return fixed non-disclosing failures within explicit direct-read ceilings, including maximum indication and notification ancestry across repository reconstruction. The implementation adds no logging surface and keeps company identifiers, notes, subjects, template bodies, and backend keys out of URLs and public errors. Independent review findings were resolved in the feature branch. Commits `fb217ee`, `7b15c96`, and `2e1f8bc` contain the rebased implementation and notification-evidence hardening. Integrated `npm run validate` passes lint, typecheck, production build, all 1008 tests, the 12,322-byte `AGENTS.md` limit, a valid 54-task graph before archival, clean Sites instance boundaries, and a clean 230-file runtime-secret scan. Residual uncertainty: hosted owner detail rendering and rejection remain `TASK-104` and `TASK-103`.

- **TASK-102:** Persist the owner indication-review collection. Depends on: `TASK-073`, `TASK-097`. DoD: one owner-bound AittaDB repository pages bounded opaque review summaries from participant indication records without placing a subject, company identifier, or note in URLs, cursors, logs, or public failures; anonymous, non-owner, malformed cursor, oversized page, corruption, restart, and finite-read repository tests plus full validation pass.

  Acceptance evidence: one configured-owner capability now pages bounded opaque indication summaries from persistent participant indication records and authenticates both public review identifiers and private continuation cursors without embedding a subject, company identifier, note, or backend cursor in URLs or public failures. Lifecycle summaries derive active, withdrawn, and rejected state from validated immutable history across repository reconstruction. Anonymous and non-owner bindings stop before storage; malformed or oversized requests, crossed identifiers, corrupt records, looping continuation, and maximum-page reads fail within explicit finite ceilings. The storage application factory exposes only the frozen named review capability and no generic adapter surface. Independent review findings were resolved before integration. Commits `bc617f6` and `d82941b` contain the rebased implementation and navigation hardening. Integrated `npm run validate` passes lint, typecheck, production build, all 996 tests, the 12,322-byte `AGENTS.md` limit, a valid 55-task graph before archival, clean Sites instance boundaries, and a clean 228-file runtime-secret scan. Residual uncertainty: the owner detail, rejection action, and hosted moderation composition remain `TASK-135`, `TASK-103`, and `TASK-104`.

- **TASK-074:** Publish the sanitized live aggregate. Depends on: `TASK-073`. DoD: public HTML and hypermedia read only the configured published aggregate projection, hide disabled or zero totals as configured, label displayed values self-declared, unverified, and non-binding, update consistently after every indication lifecycle change, and expose no identity, company, note, private count, moderation, or backend detail; focused rendering and concurrency tests plus full validation pass.

  Acceptance evidence: the public campaign reader now combines only the immutable published campaign policy and the closed sanitized aggregate projection through bounded revision sampling. Disabled, hidden, zero, unpublished, malformed, and persistently racing states expose no total, while accepted totals preserve safe integer minor units exactly and carry configured labels plus explicit self-declared, unverified, and non-binding qualification. Native HTML and versioned hypermedia render the same amount, currency, optional target status, and omission state with no participant, company, note, private count, moderation, contribution, or backend field. Schema-4 published campaigns remain compatible without requiring an aggregate record, and current indication revisions update the public projection without process memory. The rebase conflict resolution preserved the completed `/participant` onboarding path alongside the public aggregate capability. Commits `ff0f0f7`, `b41a36b`, and `68daeee` contain the reviewed implementation and compatibility hardening. Integrated `npm run validate` passes lint, typecheck, production build, all 986 tests, the 12,322-byte `AGENTS.md` limit, a valid 56-task graph before archival, clean Sites instance boundaries, and a clean 227-file runtime-secret scan. Residual uncertainty: the exact hosted investment and aggregate journey remains `TASK-139`.

- **TASK-093:** Complete the signed-in participant entry flow. Depends on: `TASK-090`, `TASK-091`, `TASK-092`. DoD: first sign-in leads to registration instead of a dead participant route, successful registration makes the current package and permitted self-service controls available on the next trusted projection, registered and deletion-requested entry states expose only their allowed actions, and equivalent HTML/JSON end-to-end tests plus full validation pass.

  Acceptance evidence: a signed-in non-owner without a profile now reaches the negotiated registration resource instead of a dead participant route, but only while the persisted public campaign is published and its applicable participation path is open. Successful registration is reflected by the next trusted participant projection and immediately exposes the current package, acknowledgment state, profile self-service, and only the founder or investment paths permitted by campaign policy. Registered and deletion-requested states derive their HTML links and hypermedia actions from the same capability model; deletion-requested participants retain only explicitly allowed independent controls. Projection failures, closed or unpublished campaigns, configured owners, foreign subjects, and missing records remain fixed and non-disclosing. Commits `b3a3718` and `264d974` contain the reviewed entry flow and hardening. Integrated `npm run validate` passes lint, typecheck, production build, all 968 tests, the 12,322-byte `AGENTS.md` limit, a valid 57-task graph before archival, clean Sites instance boundaries, and a clean 224-file runtime-secret scan. Residual uncertainty: live public onboarding and package access remain the hosted acceptance proof in `TASK-137`.

- **TASK-154:** Add atomic read-revision assertions to the storage transaction contract. Depends on: `TASK-088`. DoD: `StorageAdapter` transactions can include a bounded non-mutating check that atomically requires one key's exact revision or absence alongside writes, preserves ordered unchanged record-or-null evidence, idempotency, duplicate-key rejection, request limits, and rollback, and fails closed on unsupported discovery or malformed results; deterministic, AittaDB-adapter, hosted synthetic, policy-race, documentation, and full-validation tests pass.

  Acceptance evidence: the bounded `StorageAdapter` transaction contract now includes a non-mutating check for one exact record revision or exact absence. Deterministic and AittaDB protocol adapters preserve ordered unchanged record-or-null evidence, include checks in idempotency fingerprints and mutation ceilings, reject duplicate keys and unsupported discovery before transport, and roll back every write when a check fails. Closed protocol decoding rejects malformed check requests, result order, keys, revisions, values, replay markers, capabilities, and response budgets without exposing whether an inaccessible record exists. Hosted synthetic policy-race tests prove a stale campaign revision cannot commit adjacent writes. Commits `84ef3b8` and `c678d19` contain the reviewed implementation and adversarial contract hardening. Independent review found no remaining defect, and integrated `npm run validate` passes lint, typecheck, production build, all 961 tests, the 12,322-byte `AGENTS.md` limit, a valid 58-task graph before archival, clean Sites instance boundaries, and a clean 225-file runtime-secret scan. Residual uncertainty: the unchanged contract still requires hosted acceptance proof in `TASK-131`, while higher-level staged composition remains `TASK-156`.

- **TASK-105:** Compose the persistent owner audit-history resource. Depends on: `TASK-067`. DoD: a bounded AittaDB audit reader and owner-authorized route page canonical closed stored events through equivalent HTML and hypermedia without interpreting unknown evidence as trusted detail; malformed cursors, oversized pages, unknown evidence, anonymous, non-owner, corruption, restart, and private-detail failures remain finite and non-disclosing, and full validation passes.

  Acceptance evidence: a named bounded AittaDB audit reader now pages canonical closed stored events through one configured-owner route with equivalent HTML and hypermedia resources. Opaque cursors and page ceilings are validated before storage work, unknown or private evidence is never reinterpreted as trusted detail, and anonymous, non-owner, malformed, oversized, corrupt, missing, reconstruction, and private-detail paths remain finite and fixed without disclosure. Owner navigation advertises the resource only when the runtime capability is installed, and the storage factory exposes only the narrow audit-reader capability. Integration commits `a448e34` and `b39a8a6` preserve the reviewed implementation and follow-up hardening. Combined `npm run validate` passes lint, typecheck, production build, all 953 tests, the 12,322-byte `AGENTS.md` limit, a valid 55-task graph before archival, clean Sites instance boundaries, and clean runtime-secret checks. Residual uncertainty: persistent notification history and complete producer coverage remain `TASK-106` and `TASK-136`, while live AittaDB paging remains hosted acceptance work.

- **TASK-097:** Bound persistent investment indication records. Depends on: `TASK-061`, `TASK-066`. DoD: the production subject-bound indication repository stores bounded current metadata, immutable transition records, and chunked versioned field payloads instead of cumulative snapshots; create, edit, withdraw, reactivate, owner rejection, historical configuration, maximum size and revision, corruption, observed read and transaction budgets, retry, restart, and foreign-access contract tests plus full validation pass.

  Acceptance evidence: the production subject-bound indication repository now persists bounded current metadata, immutable transition records, and chunked versioned field payloads instead of cumulative snapshots. Lifecycle reads and exact retries reconstruct and verify only the bounded records required for create, edit, withdrawal, reactivation, and owner rejection, including operations created under historical amount and normalization policies. Maximum payload and revision boundaries, changed retries, corruption, hostile data envelopes, observed read and transaction ceilings, Worker reconstruction, concurrency, and foreign access fail closed without partial effects or private disclosure. Integration commits `917f991`, `add4228`, and `e20522f` preserve the reviewed feature changes. Combined `npm run validate` passes lint, typecheck, production build, all 944 tests, the 12,322-byte `AGENTS.md` limit, a valid 56-task graph before archival, clean Sites instance boundaries, and clean runtime-secret checks. Residual uncertainty: hosted lifecycle composition remains `TASK-096`, owner collection/detail and moderation remain `TASK-102`, `TASK-135`, and `TASK-103`, and live AittaDB limits remain hosted acceptance work.

- **TASK-095:** Harden the hosted investment mutation route. Depends on: `TASK-066`. DoD: the existing investment-interest HTML and hypermedia route accepts mutations only through the hosted same-origin proof session with bounded method-specific bodies and fields, consumes or replaces proof cookies exactly once as appropriate, exposes no trusted subject or credential, and passes exact-origin, malformed, oversized, replay, owner, and non-disclosure route tests plus full validation.

  Acceptance evidence: investment-interest HTML forms and hypermedia actions now enter mutations only through the hosted actor-bound proof session and exact application origin. Method-specific byte, field, and repeated-field limits are enforced before route parsing or persistence, unsupported methods do not consume a valid proof, and verified proofs are cleared or replaced exactly once across success and later fixed failures. Foreign origins fail before proof work, cloned form bodies cannot deadlock cancellation, and owner, anonymous, malformed, oversized, stale, replayed, and credential-bearing attempts expose neither trusted subjects nor hosted credentials. Commits `e611e28` through `786747f` contain the focused implementation and hardening. The exact rebased feature tree passed `npm run validate` with lint, typecheck, production build, all 893 tests, the 12,322-byte `AGENTS.md` limit, a valid 57-task graph before archival, clean Sites instance boundaries, and clean runtime-secret checks. Residual uncertainty: persistent indication representation and complete hosted lifecycle composition remain `TASK-097` and `TASK-096`, while live provider and AittaDB behavior remain hosted acceptance work.

- **TASK-092:** Wire persistent participant profile self-service. Depends on: `TASK-089`, `TASK-090`. DoD: a registered participant can use the existing HTML and hypermedia profile actions through the subject-bound repository and hosted mutation session while protected identity and registration fields remain immutable; edit, marketing-withdrawal, deletion-request, retry, stale, restart, owner, foreign, and non-disclosure tests plus full validation pass.

  Acceptance evidence: registered participants now use the existing profile HTML and hypermedia actions through one request-scoped subject-bound repository and the hosted mutation session. Profile edits preserve protected identity and registration fields, marketing withdrawal remains independent, and deletion requests close only the capabilities declared by the current participant state. Exact and delayed retries, stale projections, Worker reconstruction, malformed ancestry, owner and foreign access, body and field limits, proof consumption, and fixed non-disclosing failures are covered. The participant authorization ceiling of 551 reads and route ceiling of 23 reads share one 574-read request budget; read 575 fails before adapter access. Independent review found and repaired an impossible one-revision consent-withdrawal timestamp ancestry, then passed 73 focused tests and integrated `npm run validate` with all 883 tests, the 12,322-byte `AGENTS.md` limit, a valid 58-task graph before archival, a clean Sites instance boundary, and a clean 225-file runtime-secret scan. Residual uncertainty: signed-in entry integration remains `TASK-093`, while live AittaDB latency, quota, and recovery behavior remain hosted acceptance work.

- **TASK-098:** Harden redacted OAuth provider-availability diagnostics. Depends on: `TASK-051`. DoD: owner-only connection diagnostics distinguish a closed finite set of discovery failure phases without retaining or emitting a URL query, credential, token, response body, identity, exception, or provider detail; every status, redirect, media, size, encoding, JSON, document, and contract failure path passes focused tests plus full validation.

  Acceptance evidence: owner-only availability now reports one closed discovery phase across fetch, redirect and final-URL mismatch, exact status classes, media parsing, declared and streamed size, body, UTF-8, JSON, document shape, and discovery-contract failures. Fetch and body work are deadline- and chunk-bounded, rejected bodies are cancelled without awaiting hostile sources, followed or foreign final URLs fail as `status_redirect`, and fixed evidence retains no provider URL query, credential, token, body, identity, exception, or provider detail. Independent review repaired the timeout, cancellation, and final-URL gaps. Integration commits `3dce20e` and `626a897` preserve the reviewed changes; 127 focused OAuth tests pass, and integrated `npm run validate` passes lint, typecheck, production build, all 868 tests, the 12,322-byte `AGENTS.md` limit, a valid 59-task graph before archival, a clean Sites instance boundary, and a clean 225-file runtime-secret scan. Residual uncertainty: exact hosted issuer reachability remains `TASK-099`.

- **TASK-091:** Wire persistent participant access registration. Depends on: `TASK-068`, `TASK-089`. DoD: the hosted Worker exposes the existing registration HTML and hypermedia resource only to a signed-in non-owner, maps notices only from the persisted campaign policy, persists through the subject-bound repository and hosted mutation session, and passes exact-origin, proof, retry, duplicate, restart, foreign, owner, non-disclosure, and full-validation tests.

  Acceptance evidence: the hosted Worker now resolves `/participant/registration` only for the exact signed-in non-owner account, maps process and optional marketing notices from the persisted private campaign revision into immutable versioned evidence, and opens one fresh subject-bound `StorageParticipantRepository` plus the hosted browser-mutation session. Registration stores the exact acknowledged notice snapshots, validates malformed operation IDs before claiming replay authority, recovers only an exact committed retry across policy change or Worker reconstruction, and rejects stale evidence for new work. Current profiles later than revision one require their matching immutable registration anchor, and every participant request shares one finite 559-read counter across authorization and selected route work; read 560 fails before adapter access. Exact-origin, CSRF, cookie consumption, malformed and oversized bodies, duplicate and changed retry, concurrent registration, restart, owner, foreign, corruption, and non-disclosure paths are covered. Focused maximum-read tests pass 3 of 3, the repository-factory tests pass 6 of 6, and integrated `npm run validate` passes lint, typecheck, production build, all 829 tests, the 12,322-byte `AGENTS.md` limit, a valid 60-task graph before archival, a clean Sites instance boundary, and a clean 225-file runtime secret scan. Residual uncertainty: participant profile composition and signed-in entry integration remain `TASK-092` and `TASK-093`, while live network, quota, and recovery behavior remain hosted acceptance work.

- **TASK-073:** Compose atomic investment lifecycle transactions. Depends on: `TASK-061`, `TASK-066`, `TASK-067`. DoD: an AittaDB-backed subject index and mutation coordinator commit one indication transition, constant-read aggregate delta, audit event, and immutable retry receipts together or leave all unchanged; personal and company uniqueness, changed and delayed retry, concurrency, rollback, malformed-result, aggregate-corruption, restart, and foreign-access tests plus full validation pass.

  Acceptance evidence: `StorageParticipantInvestmentInterestRepository` stages one indication transition, constant-read aggregate delta, closed audit append, bounded subject index, and immutable operation receipt before committing them through one outer AittaDB transaction. Personal and normalized-company uniqueness remain non-disclosing, the participant index has an observed 100-item ceiling, unrelated indications do not increase aggregate work, and exact concurrent or delayed retries reconstruct only strictly verified immutable indication, aggregate, audit, index, and receipt state. Changed retries, stale state, aggregate corruption, malformed result envelopes, reordered or altered records, injected transaction failure, restart, and foreign access fail closed without partial effects. The repository remains a named backend-only capability with no generic adapter surface. Independent review found no TASK-073 defect. Thirty-two focused repository, aggregate, indication, and factory tests pass, and integrated `npm run validate` passes lint, typecheck, production build, all 797 tests, the 12,322-byte `AGENTS.md` limit, a valid 61-task graph before archival, a clean Sites instance boundary, and a clean 224-file runtime secret scan. Residual uncertainty: compact bounded indication history remains `TASK-097`, hosted route composition remains `TASK-096`, and live AittaDB consistency and quota behavior remain hosted acceptance work.

- **TASK-071:** Persist bounded founder applications. Depends on: `TASK-061`, `TASK-067`. DoD: the AittaDB-backed subject-bound repository persists one participant's create, edit, history, and withdrawal lifecycle against versioned contribution choices without scanning another subject or an unbounded history; investor state remains independent; retry, stale, corruption, size, read-budget, concurrency, restart, and foreign-access repository tests plus full validation pass.

  Acceptance evidence: the production repository stores bounded current metadata, immutable transition records, and chunked field payloads behind one subject-bound participant request scope. Create, edit, withdrawal, exact retry, changed retry, stale state, concurrency, contribution-choice evolution, maximum payload and revision, hostile record and transaction-result corruption, restart reconstruction, owner and foreign access, and investment independence fail or succeed through fixed bounded contracts. Hosted composition uses the same trusted participant access projection, configured campaign choices, mutation session, and shared request read budget; later participant-runtime hardening was preserved while resolving the reviewed branch integration. An independent review found no TASK-071 defect. Sixty-eight focused repository, route, factory, and hosted-composition tests pass, and integrated `npm run validate` passes lint, typecheck, production build, all 779 tests, the 12,322-byte `AGENTS.md` limit, a valid 62-task graph before archival, a clean Sites instance boundary, and a clean 223-file runtime secret scan. Residual uncertainty: synthetic services do not prove live AittaDB latency, quotas, or restart behavior; owner founder collection and detail proofs remain separately open as `TASK-133` and `TASK-134`.

- **TASK-090:** Compose the persistent participant-access projection. Depends on: `TASK-069`, `TASK-089`. DoD: the hosted runtime derives one trusted subject's participant authorization only from that subject's persisted profile plus the current package and acceptance gate, remains fail-closed across package or gate races and restarts, grants no capability to owners or unrelated subjects, and focused projection and hosted-composition tests plus full validation pass.

  Acceptance evidence: the hosted Worker now creates one fresh request-scoped, subject-bound application repository factory and derives participant authority only from an exact persisted subject and provider email label, current package head, material-acceptance gate, and matching acknowledgment. Two finite consistency attempts fail closed across changing profile, package, or gate state; owners, changed-email identities, unregistered accounts, deletion-requested accounts, and unrelated subjects receive no participant capability or private disclosure. Immutable package subtrees retain their logical ancestry charge across request-local caching, and one shared 555-read ceiling covers authorization plus route work without cache recharge. Alternating and concurrent requests for distinct subjects through one runtime remain isolated, and HTML and hypermedia JSON consume the same authorization projection. An independent review found no remaining defect and passed 75 focused checks, including an exact-limit hostile-ancestry probe. Integrated `npm run validate` passes lint, typecheck, production build, all 742 tests, the 12,322-byte `AGENTS.md` limit, a valid 25-task graph after archival, a clean Sites instance boundary, and a clean 223-file runtime secret scan. Residual uncertainty: synthetic AittaDB coverage cannot prove live network latency, quotas, token renewal, or edge failure behavior near the read ceiling; those remain hosted acceptance work.

- **TASK-089:** Harden the persistent participant-profile repository. Depends on: `TASK-064`, `TASK-067`. DoD: one subject-bound AittaDB repository atomically stores the current profile and immutable revisions for registration, profile edits, marketing withdrawal, and deletion-request intent; exact retries, changed reuse, stale and concurrent writes, restart reconstruction, malformed adapter records and results, owner or foreign subjects, and non-disclosure pass focused repository tests plus full validation.

  Acceptance evidence: `StorageParticipantRepository` now binds current and immutable-history keys to one trusted account subject through separate one-way identifiers and stores each registration, profile edit, marketing withdrawal, or deletion-request transition in one two-record compare-and-set transaction. Every current read must match its immutable revision. Existing-profile mutations validate the live current record and its corresponding history before any write; malformed, foreign, missing, crossed, or history-mismatched state reaches no transaction. Exact operation retries survive repository reconstruction and commit-then-lost responses, including after later revisions, while changed operation reuse and ordinary stale requests fail before another transaction. Closed data-only validation bounds stored documents to 8 KiB of UTF-8, 64 JSON nodes, and eight nested levels and rejects custom prototypes, accessors without invoking them, symbols, sparse or decorated arrays, extra fields, malformed envelopes, crossed subjects or revisions, wrong result counts/order/keys/values, and non-boolean replay evidence. Adapter errors preserve only fixed storage codes and discard causes. The module exports only the production-neutral storage repository and its development compatibility name; deletion persists one participant state plus an application-level withdrawal intent without writing founder or indication collections. Focused repository tests pass 70 of 70, two independent review rounds found no remaining implementation defect, and integrated `npm run validate` passes lint, typecheck, production build, all 720 tests, the 10,652-byte AGENTS limit, a valid 23-task graph before archival, a clean Sites instance boundary, and a clean 223-file runtime secret scan. Hosted authorization, registration, and profile route composition remain isolated in TASK-090, TASK-091, and TASK-092.

- **TASK-069:** Wire persistent package management, reading, and acknowledgment. Depends on: `TASK-066`, `TASK-067`. DoD: the hosted Worker exposes the owner package workspace and participant package/acknowledgment resources through AittaDB-backed version, section, acceptance, and audit repositories; immutable versions survive restarts, private drafts never enter public output, material acceptance gates are current, and route capability, concurrency, non-disclosure, and full validation tests pass.

  Acceptance evidence: complete hosted AittaDB runtime composition now injects one credential-bound owner package workspace plus subject-bound package-reader and current-acknowledgment capabilities into their existing HTML and hypermedia routes. An immutable pre-stage intent binds the actor, operation ID, expected revision, fingerprint mode and value, prior package and acceptance-gate state, original validated server timestamp, and the complete normalized mutation. Deterministic private section and Unicode-safe chunk records stay within advertised AittaDB limits; one final atomic transaction makes the version manifest, owner head, current acceptance gate, and owner audit event reachable together. Exact retries recover across partial staging, Worker reconstruction, advancing clocks, and lost final responses, while changed work conflicts before another stage. Closed data-only record and transaction-result validation rejects extra fields, custom prototypes, accessors, symbols, sparse or decorated arrays, wrong ordering, and mismatched keys, revisions, or values. Participant reads require a trusted subject-bound access capability, never expose drafts, and compare bounded gate samples around the latest acknowledgment; acceptance writes compare-and-set the unchanged current gate so stale acceptance cannot race publication. Package routes narrow the shared browser-mutation byte, field, and repeated-field ceilings before proof claim. Focused package tests pass 25 of 25, the combined hosted campaign/package composition suite passes 34 of 34, independent re-review reports no findings, and integrated `npm run validate` passes lint, typecheck, production build, all 655 tests, the 10,652-byte AGENTS limit, a valid 24-task graph before archival, a clean Sites instance boundary, and a clean 223-file runtime secret scan. Interrupted operations can leave bounded unreachable immutable intent, section, or chunk records for deployment retention cleanup; participant package access remains intentionally unavailable until TASK-090 supplies its persistent access projection.

- **TASK-068:** Wire persistent owner setup and campaign publication. Depends on: `TASK-062`, `TASK-067`. DoD: the hosted Worker exposes owner setup, campaign editing, draft preview, publish, and unpublish only through an AittaDB-backed atomic campaign/audit repository; public reads use only the separately stored published projection; restarts and concurrent revisions preserve history and retry behavior; incomplete deployment readiness blocks publication; and focused hosted composition tests plus full validation pass.

  Acceptance evidence: complete hosted AittaDB runtime configuration now injects one credential-bound `StorageCampaignRepository`, its closed public-presentation reader, deployment-readiness check, and actor-bound browser-mutation session into the existing owner setup and campaign editor resources. Setup creation, editing, draft preview, publication, and unpublication use atomic campaign/audit transactions; public rendering reads only the separately stored published projection and scalar fixtures are suppressed whenever hosted runtime values are present. Private schema version 4 stores a bounded setup manifest over deterministic immutable SHA-256-addressed chunks. Before any chunk write, an immutable operation intent binds audited mode, owner actor, operation ID, expected and resulting revision, timestamp, complete setup hash/byte/chunk topology, and transition. Exact retries recover across mid-stage interruption, restart, and committed-response loss, while changed setup, actor, timestamp, revision, transition, or audited mode conflicts before further staging. Every intent, chunk, final campaign, public projection, and audit transaction result requires a closed data-only envelope with an exact boolean replay marker and exact record count, order, key, revision, and value; custom prototypes, accessors, symbols, sparse arrays, and malformed nested records fail closed, while canonical null-prototype JSON remains supported. Setup and editor routes have distinct bounded body/field ceilings and clear a consumed mutation cookie on every post-verification response. Focused campaign persistence and composition tests pass 113 of 113. Independent review findings were repaired, and integrated `npm run validate` passes lint, typecheck, production build, all 643 tests, the 10,652-byte AGENTS limit, a valid 25-task graph before archival, a clean Sites instance boundary, and a clean 223-file runtime secret scan. Interrupted operations can leave unreachable immutable intent or chunk records for a separately bounded retention cleanup; legacy private schema version 3 fails closed, and no production persistent campaign existed at this migration boundary.

- **TASK-070:** Wire persistent participant registration and profile access. Depends on: `TASK-064`, `TASK-067`, `TASK-069`. DoD: first sign-in reaches registration instead of a dead participant route, successful registration immediately grants only that subject the current package and permitted self-service controls, profile and access projections survive restarts, owners and unrelated subjects cannot acquire participant data, and HTML/JSON end-to-end tests plus full validation pass.

  Planning evidence: this unfinished umbrella combined five independently reviewable primitives and would have serialized work that does not consume the same contract. It is retired unchanged into `TASK-089` (subject-bound profile persistence), `TASK-090` (participant-access projection), `TASK-091` (registration composition), `TASK-092` (profile composition), and `TASK-093` (signed-in entry integration). Dependencies now follow the narrow contract each downstream task actually consumes, allowing repository, founder, investor, and participant-UI lanes to proceed in parallel when their direct prerequisites are complete. No product behavior changed in this planning-only increment.

- **TASK-067:** Compose the secure hosted AittaDB application runtime. Depends on: `TASK-057`, `TASK-060`, `TASK-088`. DoD: the production entry point creates one request-safe service-token provider, AittaDB adapter, repository factory, mutation-session capability, and fail-closed runtime configuration from hosted values only; partial or malformed configuration advertises no private workflow; client credentials and access tokens stay inside backend closures; no direct D1, R2, browser-storage, process-memory, hostname, owner, or campaign fallback becomes production persistence; and composition tests, docs, secret scans, and full validation pass.

  Acceptance evidence: the production entry point now installs an all-or-nothing hosted resolver that validates the exact application origin, logical and optional transport origins, same-issuer storage entry, dedicated service client, canonical storage scopes, and independent non-extractable browser-mutation key. Application and OAuth identities, secrets, and keys must be pairwise distinct. One immutable runtime per Sites environment closes over one deadline-bounded, retryable service-token provider, one bounded `AittaDBStorageAdapter`, one named-capability repository factory, and one browser-mutation session. The factory exposes no generic builder or adapter accessor; its first capability atomically creates expiry-only one-way replay records and strictly rejects extra, accessor-backed, or malformed transaction results. Credentials and tokens remain backend closure state, the hosted runtime never enters route context, Vinext receives only `ASSETS` and `IMAGES`, and participant authorization is an explicit composition dependency rather than an environment or process-memory fallback. Complete configuration adds no private route, header, navigation control, or development-storage fallback. The runtime scanner checks production source, built artifacts, inert env examples, instance identities, committed-secret patterns, and synthetic canaries; a built-Worker test also proves responses and captured logs contain none of its injected credential, token, or key values. Focused timeout, composition, pairwise-separation, exact-result, rendering, and disclosure tests pass, and an independent security re-review found no issue. Full `npm run validate` passes lint, typecheck, production build, all 500 tests, the 10,652-byte AGENTS limit, a valid 21-task graph, the clean Sites instance boundary, and a clean 223-file runtime secret scan. Hosted storage activation remains separately gated by TASK-059 and TASK-080.

- **TASK-062:** Add the owner initial-setup resource. Depends on: `TASK-086`, `TASK-087`. DoD: an authorized owner can create and revise every setup field through equivalent structured HTML and versioned hypermedia actions, preview without publication, and receive objective readiness blockers; every mutation enforces owner identity, exact origin, CSRF, bounds, revision, idempotency, and atomic audit persistence; no raw JSON editor or reusable campaign default is required; and focused route/UI tests plus full validation pass.

  Acceptance evidence: `/owner/setup` now derives private native HTML and versioned hypermedia JSON from one owner-only campaign-setup capability. An absent deployment exposes no campaign values or reusable defaults; the owner supplies every public presentation, phase, country, amount, aggregate, founder-choice, notice, privacy, retention, and review field through structured controls or equivalent bounded JSON fields. The same resource supports revision, reports objective phase, policy, and deployment readiness blockers, and links to `/owner/setup/preview`, which renders the saved draft through the visitor presentation without persisting publication. Every write requires the trusted configured owner, exact origin, actor-bound CSRF proof, exact bounded shape, stable operation ID, compare-and-set revision, unchanged publication state, and an explicitly atomic campaign/audit repository. Exact retries reuse the original timestamp and evidence even across operation-lookup and current-state races; changed reuse, stale state, published creation, non-atomic persistence, and foreign access fail closed. UTF-8 action bounds remain representable after persistence, and the maximum structured form submits 32 phases and 64 contribution choices while supporting root-relative media and complete 128-character IDs. Twelve focused route/resource/responsive-style tests plus the campaign repository suite provide 26 passing tests, including deterministic route and repository concurrency regressions. Independent review found no remaining issue. Full `npm run validate` passes lint, typecheck, production build, all 481 tests, the 10,652-byte AGENTS limit, a valid 22-task graph, and the clean Sites instance boundary. Production route injection and AittaDB-backed runtime composition remain the separate TASK-068 increment.

- **TASK-088:** Implement the bounded AittaDB hypermedia `StorageAdapter`. Depends on: `TASK-057`, `TASK-058`. DoD: a backend-only adapter obtains bearer tokens only through an injected closure, discovers and follows exact same-issuer hypermedia actions with optional backend transport mapping, bounds and strictly validates every request and streamed response, passes the unchanged deterministic authorization, consistency, uniqueness, pagination, idempotency, quota, rollback, and non-disclosure contract suites, and redirect, media, size, retry, token-redaction, documentation, and full-validation checks pass without any instance value or credential in source.

  Acceptance evidence: `AittaDBStorageAdapter` now receives only an exact logical issuer, discovered entry resource, optional backend transport origin, injected token closure, Worker-compatible fetch, and finite software timeout. It validates same-issuer hypermedia controls and exact page identities, maps only the network origin, manually rejects redirects, and bounds token acquisition, fetch, declarations, streamed bytes, chunks, elapsed time, UTF-8, and JSON. Transaction input is copied once into an immutable data-only snapshot before serialization; accessors, custom or sparse arrays, unknown mutations, malformed keys, duplicate keys, non-finite JSON, and advertised record or transaction limit violations fail before transport. Rejected response bodies are cancelled nonblockingly, including redirects, final-URL mismatches, media errors, and malformed or oversized declarations. Failed concurrent discovery is normalized and remains retryable, and fixed failures retain no credential, body, transport detail, or cause. The unchanged shared adapter contract passes against the production adapter and deterministic hypermedia service, including authorization equivalence, consistency, uniqueness, pagination, idempotency, quota, rollback, and non-disclosure. Forty-one focused protocol and adapter tests cover transport mapping, request snapshots, response bounds, cancellation, retry, ordering, and redaction. Full `npm run validate` passes lint, typecheck, production build, all 481 tests, the 10,652-byte AGENTS limit, a valid 23-task graph, and the clean Sites instance boundary.

- **TASK-064:** Add participant profile self-service. Depends on: `TASK-063`. DoD: a registered participant can read and edit only declared editable profile fields, withdraw marketing consent independently, inspect acknowledgment status, and request account deletion through separate equivalent HTML and hypermedia actions; identity, registration-only, and system fields remain immutable; stale, retry, foreign, CSRF, origin, and non-disclosure paths pass; and documentation plus full validation succeed.

  Acceptance evidence: `/participant/profile` now derives equivalent private HTML and versioned hypermedia from one subject-bound profile, repository revision, and trusted current-package acknowledgment projection. Separate guarded actions edit only display name, country, declared interest, and participation context; withdraw optional marketing consent; or request account deletion. Identity, provider email, process acknowledgment, consent grant, deletion system state, and timestamps never become editable fields. A deletion request closes editing and repeat deletion while preserving an independent outstanding marketing withdrawal. Every mutation enforces participant actor binding, exact origin, CSRF, exact bounded fields, operation ID, compare-and-set revision, stable replay, and fixed non-disclosing failures. Complete persisted state is revalidated before any clock, operation ID, CSRF proof, or write; a regression fixture proves malformed storage reaches none of the repository mutation methods. Nine focused route and responsive-resource tests plus 30 adjacent participant tests pass. Full `npm run validate` passes lint, typecheck, production build, all 434 tests in the integrated tree, the 10,652-byte AGENTS limit, a valid task graph, and the clean Sites instance boundary.

- **TASK-066:** Add current-package acknowledgment. Depends on: `TASK-065`. DoD: an authorized participant can accept only the trusted current package requirement through equivalent CSRF-protected HTML and hypermedia actions, retries return the same immutable evidence, material changes require renewed acceptance while editorial versions preserve prior satisfaction, and forged hashes, versions, subjects, origins, stale state, and foreign access fail without mutation; focused tests and full validation pass.

  Acceptance evidence: `/participant/package/acknowledgment` now derives one private HTML form and versioned hypermedia action from the same validated participant/package state. Browser input contains only a server-issued operation ID; subject, current package version, content and requirement hashes, acceptance time, and compare-and-set revision remain server-derived. Exact retries recover the same immutable evidence, editorial versions retain satisfaction, material versions require a fresh acceptance, and satisfied resources issue neither an operation ID nor a CSRF proof. Authorization, exact-origin, CSRF, body, media, stale-projection, forged-field, foreign-evidence, persistence-failure, and non-disclosure paths fail without mutation. Eight focused route, representation, lifecycle, and responsive-style tests pass. Full `npm run validate` passes lint, typecheck, production build, all 434 tests in the integrated tree, the 10,652-byte AGENTS limit, a valid task graph, and the clean Sites instance boundary.

- **TASK-087:** Support atomically audited initial campaign creation. Depends on: `TASK-058`. DoD: the atomic campaign/audit repository accepts only an absent-to-draft `created` transition at revision one, stores campaign setup, private history, public projection, operation replay evidence, and one owner audit event in a single transaction, rejects mislabeled, published, stale, and changed-retry creation without side effects, and focused repository tests plus full validation pass.

  Acceptance evidence: the atomic campaign/audit repository now accepts `created` only when no campaign exists, the expected revision is absent, the resulting setup is revision one, and its public presentation remains unpublished. One adapter transaction stores current setup, immutable history, direct operation evidence, the unpublished public projection, and one owner-attributed audit event; exact retries return the same five-record result. Focused tests prove success, retry, mislabeled and already-published creation rejection, stale creation, changed retries, and injected audit failure with no residual campaign, history, projection, operation, or audit record. Full `npm run validate` passes lint, typecheck, production build, all 417 tests, the 10,652-byte AGENTS limit, a valid task graph, and the clean Sites instance boundary.

- **TASK-086:** Add participant communication notices to campaign setup. Depends on: `TASK-061`. DoD: every setup revision requires bounded owner-supplied required-process-email and optional-marketing-consent notices with no software or campaign defaults; parsing, immutable history, material-change classification, participant-registration mapping, and public-projection exclusion are tested; existing private setup fixtures migrate explicitly; and documentation plus full validation pass.

  Acceptance evidence: campaign setup schema version 3 requires separate owner-supplied required-process-email and optional-marketing-consent notices, each normalized, bounded to 4,000 characters, and constrained to the same one-line contract enforced by participant registration. `participantRegistrationNoticesFromCampaignPolicy` is the sole mapping into that resource and supplies no fallback text. Setup history preserves communication-notice revisions immutably, changing either notice is material, legacy private schema version 2 fails closed, and sentinel values remain absent from the separately stored public campaign projection. Focused policy, repository, registration, and AittaDB campaign suites pass, followed by full `npm run validate` with lint, typecheck, production build, all 414 tests, the 10,652-byte AGENTS limit, a valid task graph, and the clean Sites instance boundary.

- **TASK-065:** Add the registered information-package reader. Depends on: none. DoD: an authorized participant can read enabled sanitized Markdown sections, version metadata, change summary, and acknowledgment text through equivalent HTML and hypermedia representations; disabled or empty sections, raw HTML, unsafe URLs, scripts, private drafts, foreign users, and unregistered users remain absent or non-disclosing; pagination and size bounds are enforced; and focused tests plus full validation pass.

  Acceptance evidence: `/participant/package` negotiates private native HTML and versioned hypermedia JSON only after the trusted signed-in actor matches an active participant projection. An injected subject-scoped repository supplies the current immutable package, whose public metadata must match the trusted participant projection before the route revalidates unique contiguous section order, safe Markdown, bounded metadata, and acknowledgment text. Both representations include only enabled non-empty sections, version time, change summary and classification, acknowledgment text, and current acceptance status; disabled drafts, raw HTML, unsafe links, scripts, hashes, storage revisions, repository keys, and participant identity remain absent. Visible-section cursors and limits are exact and finite; a page takes the largest non-empty prefix inside the 256,000-byte source ceiling and advertises a next transition only when content remains. HTML uses the shared AST renderer, external same-origin CSS, no-store/private defensive headers, and a default-deny CSP. Eight focused domain/route tests cover parity, authorization, foreign and unregistered reads, stale projections, unsafe snapshots, malformed queries, pagination, responsive CSS, and aggregate size bounds. Full `npm run validate` passes lint, typecheck, production build, all 413 tests, the 10,652-byte AGENTS limit, a valid task graph, and the clean Sites instance boundary. Current-package mutation remains the separate TASK-066 capability.

- **TASK-063:** Add participant access registration. Depends on: none. DoD: a newly signed-in non-owner can read and submit equivalent HTML and hypermedia registration resources for display name, provider-bound email label, country, founder/investor/both interest, individual/company context, required process notice, and independent optional marketing consent; identity fields cannot be supplied by the browser; duplicate, retry, validation, CSRF, origin, and non-disclosure paths pass; and documentation plus full validation succeed.

  Acceptance evidence: `/participant/registration` negotiates native HTML and versioned hypermedia JSON from one capability model for a signed-in non-owner. It displays the provider-bound email label while omitting subject and email from every browser-supplied action field, and accepts bounded display name, normalized country, founder/investor/both interest, individual/company context, required deployment-supplied process notice acknowledgment, and independently optional marketing consent. A request-scoped participant repository receives only the trusted parsed account; exact operation retries preserve the original timestamp and result, changed retries and new duplicates conflict without disclosure, and owner, anonymous, foreign, spoofed-identity, malformed, oversized, cross-origin, and invalid-CSRF paths mutate nothing. HTML uses a same-origin page-specific stylesheet and hidden proof while JSON carries the equivalent validated actor-bound proof only in its response header. Operation IDs and CSRF proofs are issued only while registration is available; actionless successful and registered responses issue neither. Seven focused route/resource tests pass, followed by full `npm run validate` with lint, typecheck, production build, all 413 tests, the 10,652-byte AGENTS limit, a valid task graph, and the clean Sites instance boundary. Production repository and hosted mutation-session composition remain explicitly deferred to the persistent wiring tasks.

- **TASK-060:** Add a hosted browser-mutation session and CSRF capability. Depends on: none. DoD: trusted Sites identity produces short-lived encrypted actor-bound CSRF state with separate hosted key material, exact app-origin and cookie binding, constant-time proof checks, safe refresh and concurrency behavior, and no process-memory authority; anonymous, foreign, expired, replayed, substituted, cross-origin, malformed, oversized, and missing-secret paths fail closed; HTML forms and JSON headers receive equivalent proofs without serializing secrets; and focused tests plus full validation pass.

  Acceptance evidence: `http/browser-mutation-session.ts` turns only an injected trusted Sites participant or owner identity into an independently named, short-lived `__Host-` capability cookie encrypted with a dedicated non-extractable AES-GCM key. Authenticated ciphertext and additional data bind actor type and subject, the exact configured HTTPS app origin, cookie name and path, issue/expiry times, and a SHA-256 CSRF hash; neither the plaintext token nor hosted key is serialized. HTML hidden fields and JSON headers feed the same bounded request guard and constant-time proof comparison. A successful request must then win an injected atomic opaque replay claim and receives a matching expired cookie; process memory and browser storage are explicitly excluded as production authority. Nine focused capability tests plus the existing mutation-security regression suite cover anonymous and foreign actors, role and origin substitution, expiry, tampering, duplicate and renamed cookies, replay, concurrent use, concurrent refresh, malformed and oversized bodies, unsuitable or missing keys, random failure, and replay-store failure. Full `npm run validate` passes lint, typecheck, production build, all 412 tests, the AGENTS limit, a valid task graph, and the clean Sites instance boundary.

- **TASK-058:** Define the atomic AittaDB storage transaction contract required by Investor App. Depends on: none. DoD: a vendor-neutral versioned hypermedia action describes bounded record reads, cursor pages, compare-and-set multi-record put/delete transactions, idempotent operation IDs, atomic rollback, quota failures, and non-disclosing authorization errors; a deterministic protocol fixture proves the complete `StorageAdapter` contract; the AittaDB project dependency is documented without application-specific server behavior; and focused tests plus full validation pass.

  Acceptance evidence: `domain/aittadb-storage-protocol.ts` defines and strictly decodes a versioned, same-origin hypermedia capability for bounded record reads, deterministic opaque-cursor pages, and ordered compare-and-set put/delete transactions. Discovery requires fixed finite limits, canonical operation IDs, unique transaction keys, atomic record-and-receipt rollback, quota preflight, and non-disclosing authorization semantics; protocol responses are projected into the unchanged `StorageAdapter` types, and every malformed or dynamic backend failure becomes a fixed storage failure without retaining backend detail. The deterministic hypermedia service and adapter fixture passes the complete shared storage contract unchanged and separately proves arbitrary discovered targets, mixed mutation ordering, canonical JSON idempotency, successful retry after quota or injected pre-commit failure, rollback of both records and receipts, and byte-equivalent denied/missing wire responses. The AittaDB dependency document specifies only the generic namespace- and scope-bound server primitive plus its acceptance proof matrix, with no Investor App workflow or deployment value. The focused protocol suite passes, followed by full `npm run validate` with lint, typecheck, production build, all 412 tests, the AGENTS limit, a valid task graph, and a clean Sites instance boundary.

- **TASK-057:** Add a runtime-only AittaDB service-client token provider. Depends on: none. DoD: an exact HTTPS issuer, confidential service-client ID and hosted secret, and a canonical least-privilege storage-scope set produce short-lived `client_credentials` tokens only inside the Worker; renewal, expiry skew, bounded responses, client authentication, scope equality, provider failures, concurrency, and redaction are tested; no browser origin, redirect URI, source default, response, exception, or log can expose a client secret or token; and documentation plus full validation pass.

  Acceptance evidence: `services/aittadb-service-token.ts` accepts only explicit exact HTTPS logical and optional backend transport origins, confidential Basic credentials, a canonical nonempty subset of the three storage scopes, a bounded expiry skew, Worker-native fetch, and a trusted clock. Its fixed `client_credentials` request follows no redirects, bounds and strictly parses the token response, requires exact returned scope equality and a short bearer lifetime, coalesces concurrent renewal, discards cached authority on clock rollback or at the skew boundary, and exposes only one backend `accessToken()` closure. Provider errors have one fixed credential-free class and retain no response body or cause; there is no request, browser origin, redirect, logging, persistence, or serialization hook. Fifty-two focused tests cover configuration, issuer/transport separation, Basic authentication, scope subsets, response bounds, renewal, failure recovery, concurrency, and redaction. Full `npm run validate` passes lint, typecheck, production build, all 412 tests, the 10,652-byte AGENTS limit, a valid task graph, and the clean Sites instance boundary.

- **TASK-061:** Complete the configurable version-one campaign setup contract. Depends on: none. DoD: one bounded setup revision explicitly contains public presentation, phases, countries, amount and aggregate rules, permitted founder and investor paths, founder contribution choices, legal and non-binding notices, privacy contact, retention text, and publication readiness without supplying campaign defaults; parsing, history, material-change classification, public projection, and private-field exclusion are tested; and documentation plus full validation pass.

  Acceptance evidence: campaign setup schema version 2 requires the existing public presentation, explicit phase/country/path rules, and amount/aggregate policy together with deployment-supplied founder contribution choices, bounded legal and non-binding notices, an HTTPS or email privacy contact, retention text, and three explicit owner-review gates. Founder choices become a publication blocker only when a founder path is enabled; incomplete phase, campaign-policy, and deployment gates remain distinct. Immutable repository history retains policy revisions, while deterministic comparison classifies public-copy-only changes as editorial and phase, amount, founder-choice, notice, privacy, retention, or review-gate changes as material. The separately stored visitor projection still returns only the published public campaign and a sentinel private legal notice is absent from it. Focused policy, readiness, repository, owner-editor, export, and hosted-adapter suites pass, followed by lint, typecheck, production build, and full `npm run validate`; the Sites instance boundary remains clean.

- **TASK-056:** Allow the unfinished-work queue to reach zero. Depends on: none. DoD: `PLAN.md` may contain zero tasks while its heading and workflow guidance remain required, malformed or checked PLAN entries still fail, completed task identifiers remain unique in `CHANGELOG.md`, focused policy tests and documentation pass, and full validation succeeds before this task is archived.

  Acceptance evidence: the plan policy now treats an empty unfinished-task graph as the valid completed state while requiring the Investor App implementation-plan heading and durable workflow guidance. It rejects checked tasks, malformed unchecked entries, duplicate open identifiers, open/completed overlap, unknown or repeated dependencies, dependency cycles, and duplicate completed identifiers. Five focused policy fixtures cover zero and nonzero valid plans plus missing structure, malformed and checked entries, and duplicate changelog IDs. `AGENTS.md` and `README.md` document zero open tasks as the target state. Full `npm run validate` passes all 312 tests with lint, typecheck, production build, a 10,652-byte `AGENTS.md`, a valid 31-task dependency graph after archival, and a clean Sites instance boundary.

- **TASK-051:** Compose the hosted owner OAuth proof capability. Depends on: `TASK-048`, `TASK-049`, `TASK-050`, `TASK-052`, `TASK-053`, `TASK-054`. DoD: the production Worker injects the owner connection route only when complete OAuth configuration, D1, and cryptographic capabilities are present; initiation and callback use the exact configured issuer, redirect URI, scopes, durable single-use claim, and closed proof sink; incomplete deployments keep the route absent, no credential enters logs or responses, and focused composition tests plus full validation pass.

  Acceptance evidence: the production entry point installs one deployment-scoped, fail-closed resolver rather than instance values. It first requires the dedicated `OAUTH_PROOF_DB` binding, then accepts only the all-or-nothing exact HTTPS app origin, issuer, callback, confidential client, canonical storage scopes, and distinct non-extractable transaction and CSRF keys. Successful composition keeps the hosted client secret inside the OAuth service closure, uses cryptographic randomness and five-minute protocol sessions, and returns only the route service plus CSRF capability. `application-worker.ts` resolves that capability per deployment environment, catches boundary failures, and advertises or dispatches the owner-only resource only when resolution succeeds; absent, partial, malformed, unbound, and foreign-callback configurations fall through with no trusted navigation header or provider traffic. The integrated Worker flow verifies discovery, exact PKCE authorization parameters, confidential token exchange, introspection, audience/scope/expiry checks, credential-free callback output, owner-free closed D1 evidence, and durable replay rejection before any second provider request. Static entry-point checks reject console use and instance hostnames. Focused composition tests pass 3 of 3, the CSP service and route tests pass 53 of 53, and full `npm run validate` passes all 307 tests with lint, typecheck, production build, a 10,515-byte `AGENTS.md`, a valid one-task PLAN graph after archival, and a clean Sites instance boundary. Real client registration and the hosted callback remain exclusively TASK-030.

- **TASK-054:** Permit the exact OAuth authorization origin through the connection form CSP. Depends on: `TASK-048`, `TASK-050`. DoD: the owner connection resource's HTML CSP allows form submission redirects only to self and the exact configured HTTPS authorization origin; every other external origin remains blocked; JSON, callback, and error responses do not widen browser form targets unnecessarily; route tests plus a real-browser redirect check prove the flow reaches the provider; and lint, typecheck, and build pass.

  Acceptance evidence: `AittaDBOAuthProofService` exposes only the authorization origin derived from its already validated canonical HTTPS issuer. The route independently revalidates that origin and adds it beside `'self'` only to an available connection page that contains a usable form. Hypermedia JSON, POST redirects, callbacks, unavailable pages, authentication failures, and every other error retain `form-action 'self'`; malformed, HTTP, path-bearing, compound, wildcard, and foreign values cannot widen the policy. All 53 focused service and route tests pass. A real Chromium form submission exercised the actual route's HTML CSP and POST `303`, moving from a temporary same-origin app harness to the configured `https://test.aittadb.com/authorize` provider resource; its provider-side `503 feature_unavailable` result was outside this CSP proof and is handled separately by TASK-030. Integrated hosted-composition tests, lint, typecheck, and production build pass.

- **TASK-053:** Reject compound SQL before Sites migration splitting. Depends on: `TASK-049`. DoD: migration staging detects every unquoted SQLite `TRIGGER` token despite comments or TEMP modifiers, refuses to emit breakpoints for compound SQL, executes each emitted breakpoint segment independently in tests, and focused tests plus lint, typecheck, and build pass.

  Acceptance evidence: Sites migration staging now lexes SQL outside SQLite string, identifier, bracket, line-comment, and block-comment contexts and rejects every standalone unquoted `TRIGGER` token before creating an output artifact. Focused bypass cases cover ordinary, `TEMP`, `TEMPORARY`, line-comment, block-comment, compact-comment, mixed-case, and newline forms, while quoted and commented uses remain valid. The generated two-table migration is split into exactly two segments; tests execute each segment independently against SQLite before checking the resulting schema. All 17 focused tests pass in the integrated worktree, followed by lint, typecheck, and a production build. Other SQLite compound constructs remain outside this deliberately bounded trigger guard.

- **TASK-052:** Remove owner-derived values from OAuth proof persistence. Depends on: `TASK-049`. DoD: transaction claims and verified-proof rows contain no plaintext, hash, keyed pseudonym, or other value derived from the owner subject; the adapter still validates service-owned input, schema and persistence tests prove the closed column set, and focused tests plus lint, typecheck, and build pass.

  Acceptance evidence: the D1 claim and verified-proof tables now omit every owner, subject, digest, pseudonym, and corresponding bind value. `D1OAuthProofStore` still validates the trusted owner subject at its input boundary and discards it before preparing either write. Schema assertions cover the exact three-column claim table and six-column proof table, while operation recording proves the exact SQL placeholders and values presented to D1. Malformed owner input fails before persistence, and the focused suite passes all 6 tests. Integrated lint, typecheck, and production build pass. No configured Investor App deployment has applied this opt-in D1 migration yet; an external development database created from the earlier unpublished migration must be recreated before using this schema.

- **TASK-050:** Add an encrypted owner OAuth CSRF session. Depends on: `TASK-045`, `TASK-047`. DoD: the owner connection resource can issue and verify a short-lived host-only `Secure`, `HttpOnly`, `SameSite=Lax` CSRF cookie bound to the trusted owner and exact app origin; form and JSON mutation discovery remain equivalent, tampering, expiry, actor mismatch, origin mismatch, and missing key material fail closed, and focused tests plus lint, typecheck, and build pass.

  Acceptance evidence: `http/owner-oauth-csrf-session.ts` accepts only a separate non-extractable 256-bit AES-GCM key and exact HTTPS app origin, issues independent 256-bit browser proofs, and places only each proof's SHA-256 hash inside authenticated ciphertext with the trusted owner subject, origin, issue time, and expiry. The cookie omits `Domain`, is scoped to `/owner/aittadb-connection`, and uses `Secure`, `HttpOnly`, `SameSite=Lax`, and a bounded five-minute lifetime; the path scope intentionally precludes the `__Host-` prefix, which requires `Path=/`. The owner route derives HTML forms and JSON actions from the same resource, returns the matching proof header and cookie only when initiation is usable, and sends both transports through the shared bounded mutation guard before discovery. Missing capability or key material, altered ciphertext, substituted proof, duplicate cookie, expiry, actor mismatch, configured-origin mismatch, request-host mismatch, and browser `Origin` mismatch all fail closed without provider traffic. Full integrated `npm run validate` passes with 281 tests, a 10,515-byte `AGENTS.md`, and a clean Sites instance boundary.

- **TASK-049:** Persist OAuth proof transactions and closed evidence in D1. Depends on: `TASK-045`. DoD: a checked-in migration and one bounded adapter provide atomic single-use transaction claims plus closed verified-proof metadata without persisting authorization codes, tokens, client secrets, PKCE verifiers, raw state, cookie plaintext, or owner identifiers; replay, expiry, write failure, and migration packaging are tested, and lint, typecheck, and build pass.

  Acceptance evidence: `D1OAuthProofStore` validates each service-owned input, atomically inserts one claim per canonical transaction fingerprint only before expiry, and appends only exact issuer, audience, normalized storage scopes, verification time, and token expiry after successful proof. Concurrent claims have exactly one winner across adapter instances; replay, expiry, malformed input, failed D1 results, and private backend exceptions fail with fixed output. The checked-in two-table migration has no credential or owner-derived columns after the corrective TASK-052 increment. Deterministic Sites staging runs only when the ignored active deployment binding declares D1, rejects unsupported compound trigger SQL, emits the Drizzle journal and breakpoints, and executes its emitted artifact against SQLite; inert and missing bindings leave no migration output. Full integrated `npm run validate` passes with 276 tests, a 10,515-byte `AGENTS.md`, and a clean Sites instance boundary. Expired-claim housekeeping is intentionally outside this bounded proof store increment.

- **TASK-048:** Parse opt-in hosted AittaDB OAuth configuration. Depends on: `TASK-046`. DoD: one strict deployment-boundary parser accepts only a complete exact HTTPS issuer, callback URL under the configured app origin and owner route, confidential client identifier and hosted secret, least-privilege storage scopes, and cryptographic key material; absent configuration keeps the capability disabled, partial or malformed configuration fails closed without exposing values, and focused parser tests plus lint, typecheck, and build pass.

  Acceptance evidence: `worker/hosted-oauth-configuration.ts` treats a deployment with no OAuth values as disabled and accepts an enabled capability only when every public and hosted-secret value is present. It binds the callback exactly to the configured HTTPS app origin and owner callback path, accepts only canonical nonempty subsets of the three storage scopes, validates confidential Basic credentials, requires distinct canonical 32-byte key materials, and imports separate non-extractable AES-GCM keys. Partial, malformed, cross-origin, over-broad, duplicate, reordered, credential-bearing, and shared-key inputs return no capability and reflect no secret value. `.env.example` documents only neutral placeholders and keeps secret values hosted. Full `npm run validate` passes with 266 tests, a 10,515-byte `AGENTS.md`, and a clean Sites instance boundary.

- **TASK-037:** Add owner review exports. Depends on: `TASK-013`, `TASK-014`, `TASK-015`, `TASK-016`, `TASK-017`, `TASK-018`, `TASK-019`, `TASK-028`, `TASK-045`, `TASK-047`. DoD: the owner can stream bounded CSV review data and JSON backups with correct download headers; exports are owner-only, audit-logged, not retained as records, and tested for spreadsheet-injection and private-cache defenses.

  Acceptance evidence: `GET /owner/exports` derives private native HTML and versioned hypermedia JSON from one owner-authorized resource with equivalent CSRF-protected `POST` actions for a review CSV and versioned JSON backup. CSV output contains bounded current participant, investment-indication, and founder-review rows and neutralizes spreadsheet formulas even after leading whitespace; JSON is explicitly a bounded sequential current-state projection of campaign, package, review, and aggregate records rather than a cross-repository atomic snapshot. Both downloads enforce exact owner identity, origin, CSRF proof, body shape, media negotiation, pagination integrity, record count, history count, per-record UTF-8 bytes, and total bytes before exposing attachment headers or partial content. Responses use fixed filenames, correct content types and lengths, `nosniff`, same-origin resource policy, no-referrer, and private no-store caching. A guaranteed durable audit append is verified before bytes become available, retaining only the export class and trusted owner actor; it proves generation rather than browser delivery and stores no content digest, rows, package content, participant fields, filenames, source URLs, or backend identifiers. Each operation is single-use, including commit-then-timeout recovery: replay returns `409` before source reads, a fresh operation is required, and intervening data changes cannot reuse old evidence. Missing or weak capabilities fail closed, and production must inject owner-scoped AittaDB readers plus guaranteed audit persistence. Focused tests cover HTML/JSON parity, authorization, CSRF, media and method handling, formula injection, private caching, bounds, looping pagination, audit failure, retry consumption, intervening mutation, and stream cancellation. Full integrated `npm run validate` passes with 262 tests, a 10,515-byte `AGENTS.md`, and a clean Sites instance boundary.

- **TASK-032:** Add the owner public-campaign editor and publication control. Depends on: `TASK-013`, `TASK-019`, `TASK-022`, `TASK-028`, `TASK-045`, `TASK-046`, `TASK-047`. DoD: the configured owner can edit every public presentation field, preview the resulting HTML, save through the campaign repository, and explicitly publish or unpublish; every HTML form has an equivalent hypermedia action and mutations enforce identity, same-origin, CSRF, validation, revision, and audit rules.

  Acceptance evidence: `/owner/campaign`, `/owner/campaign/preview`, and `/owner/campaign/publication` derive structured native HTML and versioned hypermedia JSON from the same owner-only campaign resource, action contracts, parser, and public visitor projection. Every public presentation field is represented by bounded labeled controls, including repeatable and optional sections, rather than a raw JSON editor. Presentation saves and explicit publish or unpublish transitions require trusted configured-owner identity, exact origin, CSRF proof, exact bounded fields, server-issued operation ID, expected revision, and the explicit `atomic-campaign-audit` repository capability. One transaction commits current setup, immutable history, direct retry index, public-only projection, and an allowlisted transition derived from actual publication state; stable retries return the original result, while stale, changed, forged, and partial mutations fail closed. Publication is advertised and accepted only when phase and injected deployment readiness pass, while an existing publication can always be withdrawn. Preview reuses the public renderer with private owner and participant capabilities suppressed and does not mutate state. The production Worker accepts the workspace only through explicit dependency injection, never scalar runtime configuration, and remains fail-closed until a deployment supplies persisted campaign and audit capabilities. Focused desktop 1280x900 and mobile 390x844 browser checks found all 85 controls labeled with no overflow or overlap. Full integrated `npm run validate` passes with 247 tests, a 10,515-byte `AGENTS.md`, and a clean Sites instance boundary.

- **TASK-024:** Add the participant investment-interest flow. Depends on: `TASK-013`, `TASK-014`, `TASK-015`, `TASK-016`, `TASK-017`, `TASK-019`, `TASK-045`, `TASK-046`, `TASK-047`. DoD: authenticated participants can create, view, edit, withdraw, and reactivate their permitted personal or company indications through equivalent HTML forms and hypermedia actions; authorization, acknowledgment, validation, history, and non-disclosing duplicate behavior are tested.

  Acceptance evidence: `/participant/investment-interests` and its opaque-ID detail resources derive native HTML forms and versioned hypermedia actions from one bounded capability model for personal and company create, view, edit, withdrawal, and reactivation. A request-scoped service binds the trusted participant subject, current package acknowledgment, deployment amount and kind policy, finite owned-record reader, and an explicit `atomic-indication-aggregate-audit` mutation capability; every successful transition must commit the immutable indication revision, public aggregate projection, closed audit evidence, and retry receipt together, while forced failure leaves every effect unchanged. Mutations enforce trusted identity, exact origin, CSRF proof, bounded exact fields, server-issued operation IDs, expected revisions, configured amount increments, current acknowledgment, and lifecycle policy. Stable retries preserve history, stale writes fail, foreign and duplicate-company outcomes disclose no owner, and JSON CSRF discovery appears only when actions are available. Participant-home and private-package HTML navigation plus JSON actions expose the flow only when the route is injected, the active account declared investor interest, and the trusted runtime capability is present; spoofed headers and deletion-requested accounts cannot reveal it. Focused lifecycle, repository, route, capability, parity, rollback, and non-disclosure tests pass. Desktop 1280x720 and mobile 390x844 browser checks found one heading, two complete forms, no horizontal overflow, no unrelated overlap, and readable full-width mobile controls. Full `npm run validate` passes with 181 tests, a 10,515-byte `AGENTS.md`, a clean instance boundary, and three remaining PLAN tasks.

- **TASK-034:** Add owner investment-indication moderation. Depends on: `TASK-016`, `TASK-017`, `TASK-019`, `TASK-022`, `TASK-028`, `TASK-045`, `TASK-046`, `TASK-047`. DoD: the owner can inspect permitted indication details, reject an active indication with a bounded reason, and obtain a manual-notification template; authorization, state transitions, aggregate effects, and non-disclosure are tested.

  Acceptance evidence: `/owner/investment-indications` and its opaque-ID detail resources derive private owner HTML and versioned hypermedia JSON from one capability model, use canonical deployment URLs, bound pagination, and advertise rejection only for an active indication backed by the explicit `atomic-indication-aggregate-audit-notification` repository contract. The guarded action accepts exact fields, a server-issued operation ID, expected revision, and a normalized participant-visible multiline reason; one retry-stable transaction must reject the indication, remove its aggregate contribution, update the aggregate snapshot, append closed owner audit evidence, and create the bounded manual-notification template. Result cross-checks bind all four effects to the indication and trusted owner, while weaker adapters, stale decisions, invalid input, cross-origin requests, and forced transaction failures fail closed. Focused tests cover owner-home and runtime discovery, HTML/JSON action parity, CSRF handling, terminal action removal, canonical links, bounded paging, anonymous and foreign non-disclosure, atomic aggregate/audit/notification effects, stable replay, stale writes, unsafe fields, and complete rollback. Desktop 1280x720 and mobile 390x844 browser checks found one heading, no horizontal overflow, and no overlapping decision controls. Full `npm run validate` passes with 167 tests, a 10,515-byte `AGENTS.md`, a clean instance boundary, and four remaining PLAN tasks.

- **TASK-033:** Add the owner information-package workspace. Depends on: `TASK-014`, `TASK-022`, `TASK-028`, `TASK-045`, `TASK-046`, `TASK-047`. DoD: the owner can list, create, reorder, enable, preview, and version sanitized package sections through equivalent HTML and hypermedia controls, with immutable versions and material-change metadata.

  Acceptance evidence: `/owner/package` derives its native forms and versioned hypermedia actions from one owner-only workspace resource, while `/owner/package/preview` renders only enabled sections from the same immutable package snapshot. The injected service supports initial creation, editing, reordering, enablement, acknowledgment settings, material-change classification, exact revision preconditions, deterministic operation-derived IDs, delayed retry, and historical version reopening without process-local authoritative state. Markdown is parsed at the package boundary, rejects raw HTML and unsafe URLs, and renders through an escaping syntax-tree projector under a restrictive CSP. Mutations bind the trusted configured-owner subject, exact origin, CSRF proof, effective method, transition-specific exact fields, server-issued operation ID, and revision; JSON clients receive CSRF discovery without embedding the token in documents. Canonical deployment URLs are used behind a distinct Worker origin, and owner-home capability injection remains optional when production persistence is absent. Seven focused package, route, runtime-header, parity, non-disclosure, stale-write, retry, sanitization, and immutable-version tests pass. Desktop 1280x720 browser checks of the workspace and preview found no horizontal overflow, visible script content, or competing document headings. Full `npm run validate` passes with 163 tests, a 10,515-byte `AGENTS.md`, a clean instance-boundary check, and five remaining plan tasks.

- **TASK-038:** Add owner audit and manual-notification history. Depends on: `TASK-019`, `TASK-022`, `TASK-028`, `TASK-045`, `TASK-046`, `TASK-047`. DoD: the owner can page through bounded audit events and mark manually sent notices separately from copied templates; HTML and hypermedia expose equivalent state and no secret or unbounded event data.

  Acceptance evidence: `/owner/audit-events` and `/owner/manual-notifications` expose finite cursor pages, while `/owner/manual-notifications/{notification-id}` exposes one bounded private template with its finite copy evidence and independent sent marker. Native HTML and versioned hypermedia JSON derive from the same closed collection and detail resources, use canonical deployment URLs, advertise only currently available copy or sent transitions, and provide equivalent CSRF discovery without placing the proof in JSON documents. Mutations bind the trusted configured-owner subject, exact origin, CSRF proof, method, exact two-field body, operation ID, revision, and server clock before calling the explicit `atomic-notification-audit` repository capability; copy and sent activity commit separate notification revisions and allowlisted audit events, and stale or failed writes disclose no template or cause. Focused tests cover paging bounds, canonical links, HTML/JSON action parity, owner-home discovery, authentication and foreign-owner non-disclosure, unknown fields, cross-origin rejection, copy and sent separation, action omission, stale revisions, and secret-free errors. Full `npm run validate` passes with 156 tests, a 10,515-byte `AGENTS.md`, a clean instance-boundary check, and six remaining plan tasks.

- **TASK-025:** Add the participant founder-interest flow. Depends on: `TASK-013`, `TASK-015`, `TASK-018`, `TASK-045`, `TASK-046`, `TASK-047`. DoD: authenticated participants can create, view, edit, and withdraw a founder application independently from investment indications through equivalent HTML forms and hypermedia actions; ownership, validation, history, and separation behavior are tested.

  Acceptance evidence: `/participant/founder-interest` projects one participant-owned application, immutable revision history, and currently permitted create, edit, or withdrawal transitions into native HTML forms and versioned hypermedia actions from the same bounded capability model. A request-scoped service binds the trusted actor to the injected founder repository, campaign creation policy, contribution choices, application ID, and clock; stable operation IDs recover their original timestamps from stored history instead of process memory. Mutations use the shared trusted-session, exact-origin, CSRF, body-bound, and strict-field guard, including explicitly configured repeated form fields, hidden revision controls, validation, fixed public failures, and non-disclosing foreign or missing state. Service and route source boundaries have no investment dependency, and focused lifecycle tests prove founder create, view, edit, withdrawal, delayed retry, stale writes, history, HTML/JSON parity, authorization, and investment-record separation. Full `npm run validate` passes in the integrated tree with 154 tests.

- **TASK-023:** Add the authenticated participant home state. Depends on: `TASK-014`, `TASK-015`, `TASK-022`, `TASK-028`, `TASK-047`. DoD: a valid configured identity session extends the public campaign resource with private-package and participant-home links and currently allowed actions; signed-out and foreign-subject requests disclose nothing private; HTML and hypermedia JSON expose equivalent capabilities from the same authorization service.

  Acceptance evidence: the Worker parses one trusted actor account, resolves a subject-bound participant/profile, current-package, and acknowledgment projection through an injected reader, validates that projection again at the application boundary, and replaces any client-supplied renderer header. The root campaign, `/participant`, and `/participant/package` derive HTML navigation and hypermedia links/actions from that same authorized projection; owner and participant capabilities coexist without conflating roles, while signed-out, malformed, missing, unavailable, and foreign-subject state exposes no private capability. Canonical resource URLs come from deployment configuration rather than a hard-coded hostname. Twelve focused authorization, repository-projection, routing, runtime-header, server-rendering, coexistence, and disclosure tests pass. Desktop 1280x720 and mobile 390x844 browser checks found no overflow, clipping, or header collisions and confirmed that rendered copy is campaign-facing rather than development narration. Full `npm run validate` passes in the integrated tree with 154 tests.

- **TASK-036:** Add owner aggregate reconciliation. Depends on: `TASK-017`, `TASK-019`, `TASK-022`, `TASK-028`, `TASK-045`, `TASK-046`, `TASK-047`. DoD: the owner can preview stored-versus-calculated totals and apply an explicitly confirmed correction; the mutation is idempotent, audit-logged, and unavailable when consistency guarantees are not met.

  Acceptance evidence: `DevelopmentInMemoryAggregateRepository` now exposes an explicit `atomic-aggregate-audit` correction capability that writes the preview-bound replacement, retry receipt, and allowlisted owner reconciliation event in one `StorageAdapter` transaction. Stable retries reopen both the original correction and its original audit evidence; changed retries conflict, stale confirmations fail, and a forced transaction failure leaves both aggregate and audit state untouched. The owner reconciliation URI derives equivalent HTML and hypermedia JSON from one resource and action contract, advertises correction only for a mismatch backed by that atomic capability, and applies trusted owner identity, exact origin, CSRF, bounded-body, exact-field, revision, and explicit-confirmation checks before mutation. Read-only or weaker adapters still expose the comparison but omit the action and return a fixed unavailable response to mutation. Seven focused tests cover atomic commit and rollback, audit persistence, delayed retry, HTML/JSON parity, owner-home discovery, JSON and native-form mutation, authentication and ownership, CSRF rejection, stale preview, and fail-closed consistency. Full `npm run validate` passes in the stable integration tree with 131 tests.

- **TASK-035:** Add owner founder-application review. Depends on: `TASK-018`, `TASK-022`, `TASK-028`, `TASK-047`. DoD: the owner can list and inspect founder applications through equivalent HTML and hypermedia resources while participant ownership and private fields remain non-disclosing to every other actor.

  Acceptance evidence: `DevelopmentInMemoryFounderApplicationReviewRepository` pages the same adapter-backed current founder records for the configured owner, replays each stored application through its domain history, verifies every immutable revision, and derives an opaque one-way review ID from applicant subject plus application ID so internal subjects never enter navigation URLs. The owner review route negotiates HTML or hypermedia JSON at the same collection and detail URIs from one resource projection, advertises the collection from the capability-aware owner home, bounds pagination, and returns fixed authentication, not-found, invalid, or unavailable representations. Four new focused tests cover owner paging and reopen-compatible inspection, opaque identifiers, complete collection/detail parity, owner-home discovery, anonymous and foreign rejection before repository access, bounded queries, missing details, and absence of applicant subjects or private fields from denied responses. Existing founder history and route-dispatch coverage remains green. Full `npm run validate` passes in the stable integration tree with 124 tests.

- **TASK-019:** Implement the in-memory audit and notification repositories. Depends on: `TASK-011`, `TASK-012`. DoD: deterministic in-memory audit/manual-notification repositories pass audit and notification contract tests, distinguish copied templates from sent markers, record export/reconciliation/moderation events, and redact sensitive fields from serializable evidence.

  Acceptance evidence: `DevelopmentInMemoryAuditRepository` appends retry-addressed immutable allowlisted events and returns finite cursor pages, including resource transitions, reconciliation, export-class, and manual-notification activity without retaining export contents or arbitrary detail. `DevelopmentInMemoryManualNotificationRepository` atomically compare-and-sets a private current record with each immutable history revision, keeps copy evidence distinct from an owner-entered sent marker, and reconstructs stored state through bounded domain transitions. Notification storage keys are one-way derived, prior revisions are verified on read, stable retries return their original result, and changed retries or stale writes fail. Four focused tests run the reusable audit and notification contracts across append, paging, reopen, copy, sent marking, delayed replay, history integrity, redaction, allowlist rejection, template and evidence bounds, and fixed public errors without credentials or private content. Full `npm run validate` passes in the stable integration tree with 120 tests.

- **TASK-017:** Implement the in-memory aggregate repository. Depends on: `TASK-010`, `TASK-012`. DoD: deterministic in-memory aggregate/reconciliation repository passes aggregate contract tests, updates totals from indication projections without double-counting retries, previews mismatches before correction, and returns only sanitized public aggregate fields.

  Acceptance evidence: `DevelopmentInMemoryAggregateRepository` stores one latest closed projection per indication, one compare-and-set aggregate snapshot, and immutable retry receipts through the development `StorageAdapter`. Higher indication revisions replace prior projections, identical revisions deduplicate, older revisions are classified as superseded, and conflicting same-revision facts fail without changing totals. Aggregate and projection updates commit atomically, stored mismatches block ordinary updates, and correction requires the exact confirmed reconciliation preview before a compare-and-set replacement. Four focused tests run the reusable contract across active, withdrawn, rejected, duplicate, superseded, reopened, delayed-replay, stale-write, and reopen behavior; they also cover mismatch preview, exact correction, overflow and malformed projections without partial writes, and allowlisted public output with no private contribution facts. Full `npm run validate` passes in the stable integration tree with 120 tests.

- **TASK-016:** Implement the in-memory indication repository. Depends on: `TASK-008`, `TASK-012`. DoD: deterministic in-memory personal/company indication repository passes indication contract tests, enforces one personal active indication, enforces company uniqueness without disclosure, records immutable history, and exposes idempotent withdrawal/reactivation/rejection transitions.

  Acceptance evidence: `DevelopmentInMemoryIndicationRepository` binds participant operations to the trusted actor, configured owner rejection to the configured owner subject, and amount plus acknowledgment authority to trusted deployment context. Each mutation atomically compare-and-sets the current snapshot, creates one immutable cumulative-history revision, and acquires or releases a hashed active-uniqueness lease without exposing personal or normalized company keys. Stored snapshots are reconstructed through domain transitions and verified against every immutable history prefix, storage revision, snapshot hash, operation fingerprint, and active lease. Two focused tests run the reusable lifecycle contract across personal and company creation, edits, withdrawal/reactivation/rejection, stable and changed retries, stale writes, cross-subject duplicate attempts, uniqueness release and reacquisition, material acknowledgment gates, owner authorization, non-disclosing access, history corruption, and unrelated repository state. Full `npm run validate` passes in the stable integration tree with 120 tests.

- **TASK-031:** Implement the AittaDB campaign-configuration repository. Depends on: `TASK-012`, `TASK-013`. DoD: an AittaDB adapter reads and conditionally replaces one campaign configuration record with bounded payloads, revision conflict detection, non-disclosing failures, and contract tests matching the deterministic fake adapter.

  Acceptance evidence: `AittaDBCampaignConfigurationRepository` stores the validated current campaign setup and immutable retry history in one explicitly configured AittaDB JSON record, bounds serialized writes and streamed responses, and uses exact strong `ETag` validators with `If-None-Match` creation or `If-Match` replacement. It discovers the configured issuer's OpenAPI contract before mutation and fails before `PUT` unless atomic preconditions and `412` conflicts are advertised; issuer, key, token provider, and HTTP implementation remain deployment inputs. The same campaign contract now runs against the deterministic in-memory and HTTP-backed repositories. Five focused AittaDB tests cover create, update, replay, reopen, history pagination, stale and concurrent writes, runtime URL and bearer use, unsupported-backend fail-closed behavior, byte bounds, authorization equivalence, non-disclosing errors, and malformed deployment boundaries. Full `npm run validate` passes in the stable integration tree with 120 tests. Residual dependency: an AittaDB deployment that advertises only unconditional record replacement remains read-compatible, but this repository intentionally refuses to mutate it.

- **TASK-018:** Implement the in-memory founder application repository. Depends on: `TASK-009`, `TASK-012`. DoD: deterministic in-memory founder application repository passes founder contract tests, keeps founder records separate from investment indications, records immutable history, and supports edit and withdrawal by owner subject only.

  Acceptance evidence: `DevelopmentInMemoryFounderApplicationRepository` derives current and immutable-history keys from the authenticated applicant subject, validates configured contribution choices, reconstructs stored records through the founder domain transitions, and atomically compare-and-sets each current snapshot with its cumulative history revision. Three focused tests run a reusable create/edit/withdraw contract across reopen and replay, changed retry, stale write, foreign/anonymous/denied/missing access, frozen prior history, and current-versus-history corruption; a source-boundary test proves there is no investment-indication dependency. Full `npm run validate` passes in the stable integration tree with 105 tests.

- **TASK-015:** Implement the in-memory participant repository. Depends on: `TASK-007`, `TASK-012`. DoD: deterministic in-memory participant/profile/consent repository passes participant contract tests, supports registration/update/deletion intent, exposes deletion intent for application-level coordination without importing sibling repositories, and uses non-disclosing foreign-record failures.

  Acceptance evidence: `DevelopmentInMemoryParticipantRepository` binds hashed storage keys and trusted account identity to one subject, atomically compare-and-sets the current profile with an immutable revision, and supports registration, policy-limited profile updates, marketing-consent withdrawal, and account-deletion intent. Retry-stable operation IDs return original snapshots while changed retries and stale writes fail. Five focused tests cover the reusable repository contract, reopen through adapter state, frozen snapshots, protected fields, consent and deletion state, delayed replay, stale writes, anonymous/foreign/denied/missing equivalence, non-disclosing errors, and absence of indication or founder imports. Full `npm run validate` passes in the stable integration tree with 105 tests.

- **TASK-010:** Define aggregate and reconciliation contracts. Depends on: `TASK-008`, `TASK-040`. DoD: aggregate summary, contributing-state rules, stored-versus-calculated reconciliation preview, correction confirmation, and public sanitization contracts are typed and documented; tests cover active-only contribution, rejected/withdrawn exclusion, oversubscription display data, and no identity/note exposure.

  Acceptance evidence: `domain/investment-aggregate.ts` projects closed aggregate facts from indication revisions, deterministically keeps each indication's highest revision, treats identical delivery as a retry, and includes only active amounts in integer minor units. Reconciliation previews compare stored and calculated totals/counts, while correction requires an exact explicit preview-bound confirmation before a repository compare-and-set. Six focused tests cover active-only contribution, withdrawn/rejected exclusion, retry and revision deduplication, conflicting facts, match/mismatch previews, correction confirmation, integer oversubscription states, and allowlisted public output without identities, companies, notes, lifecycle detail, moderation, or private counts. Full `npm run validate` passes in the stable integration tree with 105 tests.

- **TASK-046:** Define the browser mutation security boundary. Depends on: `TASK-004`, `TASK-028`. DoD: a reusable request guard authenticates the trusted actor and enforces allowed origin, CSRF proof, bounded body input, and non-disclosing failure mapping before mutation code runs; focused tests cover missing, malformed, spoofed, expired, cross-origin, and valid requests without embedding feature policy in the guard.

  Acceptance evidence: `http/mutation-security.ts` obtains identity only from an injected trusted session resolver, validates participant or owner subject and session expiry, requires an exact configured origin, compares a bounded CSRF proof with an expiring SHA-256 hash in constant time, and incrementally caps JSON or form bodies before returning a frozen actor/method/body boundary. Form method overrides are accepted only for form-encoded `POST`; fixed public failures disclose no token, subject, origin, body, or cause. Six focused tests cover absent and malformed sessions, spoofed identity fields and headers, expired session and CSRF state, missing proof, cross/null/malformed origins, unsupported and malformed bodies, duplicate fields, size limits, prototype-safe parsing, and valid JSON and form requests. Full `npm run validate` passes in the stable integration tree with 105 tests.

- **TASK-008:** Define investment indication contracts. Depends on: `TASK-006`, `TASK-040`. DoD: personal indication, company indication, lifecycle state, company identifier normalization, acknowledgment requirement, participant-visible transitions, owner rejection, and history-entry contracts are typed and documented; tests cover one personal active indication, company duplicate non-disclosure, withdrawal/reactivation, and material-acknowledgment blocking.

  Acceptance evidence: `domain/investment-indication.ts` defines personal and company fields, integer minor-unit amount validation, deterministic country-aware company identifier normalization, active/withdrawn/rejected lifecycles, immutable history, participant capabilities, owner rejection, and non-disclosing active uniqueness checks. Trusted package context supplies acceptance evidence and configured currency rather than request data. Five focused tests cover one personal active record, normalized company collision without disclosure, withdrawal and reactivation, material-version blocking and renewed acceptance, and terminal owner rejection. Full `npm run validate` passes in the stable integration tree with 85 tests.

- **TASK-014:** Implement the in-memory content and acknowledgment repositories. Depends on: `TASK-006`, `TASK-012`. DoD: deterministic in-memory package-version and acknowledgment repositories pass content contract tests, preserve immutable snapshots, enforce material-version acceptance requirements, and expose no private package data to unauthenticated reads.

  Acceptance evidence: adapter-backed development package and acknowledgment repositories atomically create immutable records and compare-and-set their current heads under retry-stable operation IDs. Stored package snapshots are reconstructed and hash-verified, acceptance subject and hashes come from authenticated context plus the trusted current version, and material changes renew the required acceptance. Three focused contract tests cover replay, changed retries, stale rollback, immutable history, editorial versus material versions, current-version enforcement, malicious client fields, and indistinguishable anonymous or foreign access. Full `npm run validate` passes in the stable integration tree with 85 tests.

- **TASK-013:** Implement the in-memory campaign repository. Depends on: `TASK-012`, `TASK-029`, `TASK-039`, `TASK-040`. DoD: deterministic in-memory campaign/setup/phase repository passes the campaign contract tests, rejects campaign-specific defaults, preserves setup history, and documents that it is not production storage.

  Acceptance evidence: `DevelopmentInMemoryCampaignRepository` composes one campaign setup and immutable phase/setup history over the credential-bound `StorageAdapter`, with atomic compare-and-set saves and retry-stable operation IDs. Parsing requires public campaign, phase, country, amount, currency, and aggregate choices instead of supplying reusable defaults. Five contract tests cover first save, reopen through the adapter, update, replay, pagination, immutable history, invalid setup, stale revisions, and observationally equivalent inaccessible or missing data. The implementation identifies itself as development-only and retains no repository-local durable state. Full `npm run validate` passes in the stable integration tree with 85 tests.

- **TASK-045:** Define mutation-capable hypermedia action contracts. Depends on: `TASK-022`. DoD: framework-independent action contracts support the required safe and mutating methods, request media types, bounded typed fields, and currently allowed transitions; shared tests prove HTML form and JSON action parity without coupling actions to a specific route implementation.

  Acceptance evidence: `domain/hypermedia-action.ts` validates bounded safe and mutating action definitions for `GET`, `POST`, `PUT`, `PATCH`, and `DELETE`, form and JSON request media types, four typed field families, all field locations, and fixed non-disclosing contract failures. Lazy availability prevents construction of unavailable transition targets. Five focused tests cover safe and mutating definitions, bounds and secret-value rejection, equivalent HTML and JSON projections, effective-method form handling, JSON-only controls, and caller/state-specific omission. Existing root and owner navigation actions now use the shared contract without changing their wire shape. Full `npm run validate` passes in the stable integration tree with 85 tests.

- **TASK-047:** Extract modular application route dispatch. Depends on: `TASK-022`, `TASK-028`. DoD: the Worker delegates public, participant, and owner resource requests through narrow route-handler boundaries; adding one resource does not require editing a sibling resource handler; existing root, owner, content-negotiation, image, runtime-header, and fallback behavior remains covered and unchanged.

  Acceptance evidence: `worker/application-worker.ts` owns runtime normalization plus image and application fallback dispatch, while public, participant, and owner resource handlers compose through narrow response-or-null contracts under `worker/routes/`. A participant resource can be added to its route group without modifying either sibling handler. Five focused tests cover group isolation, root and owner negotiation, authentication and non-disclosure, HTML `Vary` behavior, image separation, fallback rendering, and unchanged runtime headers. Full `npm run validate` passes with 67 tests.

- **TASK-040:** Define amount and public-aggregate display configuration. Depends on: `TASK-004`. DoD: currency, integer minor-unit minimum, increment, optional maximum, aggregate visibility, and sanitized display contracts are framework-independent with boundary and invalid-configuration tests.

  Acceptance evidence: `domain/amount-aggregate-configuration.ts` strictly parses an explicit three-letter currency, integer minor-unit minimum and positive increment, optional maximum, and hidden or non-zero public aggregate policy without defaults. Its public projection contains only amount, currency, bounded safe label, and qualifier. Seven focused tests cover complete and hidden configuration, missing and unknown fields, invalid currency and numeric boundaries, configured increments, unsafe display text, zero suppression, and exclusion of private fields. Full `npm run validate` passes with 67 tests.

- **TASK-011:** Define audit and manual-notification contracts. Depends on: `TASK-004`. DoD: audit event shape, actor attribution policy, notification-template, copied-versus-sent distinction, export event, and redaction rules are typed and documented; tests cover audit append intent, manual sent-marker behavior, and no credential or private-note fields in public errors.

  Acceptance evidence: `domain/audit-notification.ts` defines trusted-subject actor attribution, retry-addressed immutable append intents, allowlisted resource/export/notification detail, bounded private templates, distinct copy evidence and owner-entered sent markers, and an explicit redaction policy. Four focused tests cover frozen audit evidence, export events without payload retention, retry-stable copy and sent transitions, bounded templates, owner-only markers, and fixed public errors that exclude credentials and private notes. Full `npm run validate` passes with 67 tests.

- **TASK-012:** Define the `StorageAdapter` contract suite. Depends on: `TASK-004`. DoD: adapter interface, transaction/idempotency expectations, pagination boundary, compare-and-set requirement, non-disclosing failure shape, and shared contract-test harness are documented and typed; tests run against a minimal fake adapter proving the harness detects authorization, duplicate, drift, and disclosure failures.

  Acceptance evidence: `domain/storage-adapter.ts` defines a credential-bound adapter with finite pages, bounded atomic transactions, compare-and-set revisions, replay-safe operation IDs, JSON documents, and fixed non-disclosing failures. `tests/support/storage-adapter-contract.ts` is a reusable typed behavioral harness. Five tests run it against a conforming minimal fake and deliberately faulty authorization, duplicate, stale-write, and disclosure variants, proving those violations are detected rather than merely exercising a happy path. Full `npm run validate` passes with 67 tests.

- **TASK-039:** Define phase and country-eligibility configuration. Depends on: `TASK-004`. DoD: phase identity, open/closed state, enabled participation paths, country normalization, allow/deny rules, and setup-completion checks are framework-independent and tested without campaign defaults.

  Acceptance evidence: `domain/phase-configuration.ts` strictly parses stable phase identity, explicit open or closed state, canonical enabled participation paths, and normalized allow or deny country rules without supplying deployment defaults. Setup readiness and participation eligibility remain separate projections. Eight focused tests cover complete and missing setup, malformed and unknown fields, duplicate normalized countries and paths, injected country normalization, allow and deny behavior, phase state, and path eligibility. Full `npm run validate` passes with 46 tests.

- **TASK-009:** Define founder application contracts. Depends on: `TASK-004`. DoD: founder application fields, contribution-area choices, lifecycle state, separate-from-investment rule, and history-entry contracts are typed and documented; tests cover create/edit/withdraw behavior and the ability to have founder and investor records independently.

  Acceptance evidence: `domain/founder-application.ts` defines deployment-supplied contribution choices, bounded founder fields, received and withdrawn states, applicant-owned transitions, and immutable per-revision history snapshots. HTTPS profile-link validation and stable identifiers reject unsafe or ambiguous input. Five focused tests cover configurable choices, create/edit/withdraw transitions, immutable history, stale and foreign transition rejection, terminal withdrawal, and founder records changing independently from investment records. Full `npm run validate` passes with 46 tests.

- **TASK-007:** Define participant profile and consent contracts. Depends on: `TASK-004`. DoD: access-registration profile, declared interest, account email label, marketing consent, account-deletion request state, and editable-field rules are typed and documented; tests cover first-registration requirements, profile updates, consent withdrawal, and deletion-withdraws-active-interest intent.

  Acceptance evidence: `domain/participant-profile.ts` defines identity-bound account values, required first-registration fields, declared interest and context, explicit edit rules, process-email acknowledgment, independent marketing consent, and account-deletion state. Deletion emits a retry-stable application-service intent to withdraw active interests without importing sibling repositories. Four focused tests cover registration requirements and non-disclosing errors, participant-editable updates, idempotent consent withdrawal, and deletion intent behavior. Full `npm run validate` passes with 46 tests.

- **TASK-006:** Define package content and acknowledgment contracts. Depends on: `TASK-004`. DoD: package section, ordering, immutable version, content hash, material-change, acknowledgment text, and acceptance-record contracts are typed and documented; tests cover empty-section hiding, immutable snapshot creation, renewed-acceptance requirements, and unsafe Markdown rejection at the contract boundary.

  Acceptance evidence: `domain/package-content.ts` defines bounded section, immutable version, SHA-256 content-hash, material-change, acknowledgment, and acceptance-evidence contracts. Structured Markdown parsing rejects raw HTML and unsafe link or image protocols; deterministic snapshots clone, order, and freeze caller input. The required acceptance hash propagates across non-material saves so an unaccepted material version cannot be bypassed. Four focused tests cover reader projection, snapshot immutability, stable hashes, renewed acceptance, and unsafe Markdown, and the architecture records the trust and hashing boundaries. Full `npm run validate` passes with 46 tests, and `npm audit --omit=dev` reports zero production dependency vulnerabilities.

- **TASK-044:** Make the implementation plan explicitly parallel. DoD: every unfinished task records only its direct task dependencies and any external blocker; planning guidance distinguishes per-worktree task focus from global concurrency; architecture guidance prevents concrete sibling-module coupling; and documentation validation proves stable IDs, dependency references, and the `AGENTS.md` size limit remain valid.

  Acceptance evidence: all unfinished work now declares direct prerequisites in an acyclic task graph, with ten currently ready software tasks and the live OAuth proof marked by its separate external prerequisite. False navigation and concrete-repository sequencing was removed, while three small shared-boundary tasks make mutation actions, request security, and route dispatch independently implementable before parallel feature routes. `npm run plan:check` validates task formatting, stable references, uniqueness, and cycles; `AGENTS.md` defines per-worktree focus and remains 10,515 bytes against its 32,000-byte limit. Full `npm run validate` passes with the clean Sites-instance boundary, lint, strict type checking, production build, and all 25 tests.

- **TASK-042:** Create a separate acceptance Sites instance. DoD: a new owner-only Sites project is bound in an isolated worktree, receives only synthetic campaign configuration plus the configured development owner, deploys the exact validated source, remains separate from production data and access, and is ready for a later `invest-test.aittadb.com` custom domain.

  Acceptance evidence: a second Sites project was created once and bound only inside a detached acceptance worktree. Version 1 packages commit `0a0f1e2` exactly, uses the synthetic Northstar Robotics fixture and the current Sites owner as its development owner, and deployed successfully behind owner-only ChatGPT sign-in. Browser verification reached both the published campaign and authorized owner workspace. No production campaign value, storage binding, access grant, or custom domain is shared; the instance is ready for a later `invest-test.aittadb.com` domain. External visitor invitations are disabled in the current Sites workspace, so acceptance access remains limited to the Sites owner.

- **TASK-043:** Add the create-your-instance quick start. DoD: the README opening includes a compact `@Sites` prompt that interviews one question at a time, separates reusable behavior from runtime campaign settings, requests a decision review, and requires approval before building or publishing without embedding reference-campaign defaults.

  Acceptance evidence: the README now opens with a copy-ready `@Sites` prompt linking this repository. It requests compact one-question-at-a-time interviewing, configurable instance data, the core campaign and trust decisions, a decision review, private initial creation, and explicit approval before public publication. The prompt contains no reference-campaign defaults; the Markdown diff check and AGENTS size guard pass.

- **TASK-041:** Externalize per-instance Sites binding metadata. DoD: reusable source tracks only an inert hosting example, each deployment keeps its real `.openai/hosting.json` ignored and local, builds fall back safely for clean checkouts, packaging still requires an explicit active binding, and tests prove production and acceptance identifiers cannot enter normal source diffs.

  Acceptance evidence: the real production binding is no longer tracked, `.openai/hosting.example.json` contains only null placeholders, and the instance-boundary check fails if `.openai/hosting.json` enters the index. A build without the active binding passes and omits deployment metadata, while `npm run sites:package` fails closed until an exact active project identifier is restored. Runtime instance values remain in ignored local or hosted configuration.

- **TASK-029:** Load the public campaign presentation from runtime configuration. DoD: `CAMPAIGN_CONFIG_JSON` supplies campaign identity, public copy, sections, links, and visual URLs; invalid, absent, or unpublished configuration yields a generic unavailable resource; HTML, metadata, and hypermedia share one validated configuration; tests use only synthetic fixtures; and reusable source contains no AittaDB campaign copy or campaign artwork.

  Acceptance evidence: one bounded parser now validates every public presentation field, HTTPS or root-relative media, and safe links before a worker-owned header supplies the same campaign to metadata, HTML, owner status, and hypermedia JSON. Missing, malformed, oversized, insecure, and unpublished values render a generic unavailable resource. Campaign-specific source and artwork were removed; all 25 tests use a synthetic campaign and pass under `npm run validate`. Desktop and 390px mobile browser checks showed responsive, non-overlapping layouts, and `AGENTS.md` remains below 32,000 bytes.

- **TASK-028:** Configure one runtime owner identity and owner home. DoD: `OWNER_EMAIL` is validated as instance configuration, signed-out owner requests enter Sites sign-in, foreign signed-in users receive a non-disclosing denial, the matching owner can open `/owner`, root HTML and hypermedia expose owner navigation only to that actor, and focused tests cover all three identities.

  Acceptance evidence: the worker validates `OWNER_EMAIL`, replaces any client-supplied internal owner header, and independently authorizes owner HTML and JSON requests. `/owner` redirects anonymous browser requests through Sites sign-in, returns a generic denial to foreign identities, and presents an owner workspace only to the matching actor. The root resource adds `Manage campaign` in HTML and hypermedia only for that owner. Focused normalization, spoofing, anonymous, foreign-user, matching-owner, and representation-parity tests pass; `npm run validate` passes with 21 tests.

- **TASK-027:** Bind canonical metadata to the runtime app origin. DoD: `APP_BASE_URL` is validated as instance configuration with a request-origin fallback, no deployment hostname becomes a software default, HTML metadata receives the resolved origin through a worker-owned request header, focused tests prove distinct deployments produce distinct canonical and social URLs, and the AittaDB Sites instance is configured and verified separately.

  Acceptance evidence: `http/app-origin.ts` validates and normalizes optional per-instance origins, falls back to the current request origin, and replaces client-supplied internal origin headers. The metadata renderer consumes only the worker-owned value. Generic tests prove configured and fallback behavior across distinct example hosts; `npm run validate` passes with 16 tests and `AGENTS.md` remains below 32,000 bytes. Separately, the AittaDB Sites instance has `APP_BASE_URL=https://invest.aittadb.com`, and Sites reports its custom-domain routing and TLS active.

- **TASK-022:** Add same-URI hypermedia JSON for the public campaign resource. DoD: `GET /` negotiates HTML, `application/json`, and `application/vnd.aittadb-invest+json; version=0.1` without inspecting `User-Agent`; JSON exposes public campaign `data`, semantic `links`, and only currently available `actions`; unsupported explicit versions return `406`, responses vary on `Accept`, HTML and JSON share public campaign state, and focused tests prove representation parity.

  Acceptance evidence: the root worker now negotiates the human page or the `0.1` hypermedia document from `Accept`, returns `406` for unsupported explicit versions, adds `Vary: Accept`, and reports `Investor-App-API-Version`. HTML and JSON import the same public campaign fixture and sign-in action definitions. Direct negotiation tests cover browser, RSC, wildcard, weighted, compatibility JSON, vendor JSON, unsupported-version, and unsupported-media requests; worker tests prove state/action parity and identical JSON across different `User-Agent` values. `docs/HYPERMEDIA_API.md` records the route contract and `npm run validate` passes with 11 tests.

- **TASK-021:** Build the signed-out public landing for the active AittaDB investment pre-registration. DoD: the root route addresses prospective investors and founders, presents public AittaDB facts, investor and founder paths, the non-binding process and risks, and a real sign-in action; it uses checked-in AittaDB brand artwork, is responsive and accessible, contains no developer-facing prompts or roadmap copy, and has focused rendered-output tests.

  Acceptance evidence: the root route now presents the active public pre-registration with audience-facing investor and founder paths, an actual Sites sign-in transition, public product facts, process, risk disclosures, and no repository or implementation-summary copy. The responsive layout uses AittaDB's checked-in public mark and boundary artwork plus a campaign-specific social card. The rendered-output test asserts the campaign experience and rejects the former scaffold language. The private-content scan and `npm run validate` pass; `AGENTS.md` remains below its 32,000-byte limit.

- **TASK-020:** Bind the initial scaffold to one ChatGPT Sites project. DoD: the Sites project is created exactly once, its returned project identifier is persisted unchanged alongside null logical storage bindings, site metadata identifies Investor App, and no runtime secret, access-policy, or custom-domain change is bundled into the binding.

  Acceptance evidence: one owner-only Sites project was created with the Investor App title and description, and the returned opaque identifier is stored unchanged in `.openai/hosting.json` with `d1` and `r2` still null. Runtime environment, access policy, and custom domains were left unchanged. `npm run validate` passes for the bound source.

- **TASK-004:** Define shared domain foundation types. DoD: money minor-unit, timestamps, stable identifiers, actor subjects, country codes, validation-result, and non-disclosing domain-error types are documented and typed in framework-independent modules; tests cover integer money handling, amount bounds, country normalization hooks, and generic error mapping.

  Acceptance evidence: `domain/foundation.ts` defines the framework-independent branded values, constructors, validation result, actor shape, and fixed-message error mapping. Focused tests reject fractional, unsafe, negative, misconfigured, and out-of-range minor-unit amounts; exercise default and injected country normalization; validate timestamp, identifier, and subject boundaries; and prove authorization, missing-record, unknown-error, and private-cause responses do not disclose internal details. `npm run validate` passes with strict type checking included.

- **TASK-001:** Establish the initial Sites-compatible repository scaffold. DoD: app package metadata, neutral first screen, runtime environment template, FSL-1.1-MIT license, README, AGENTS, PLAN, ROADMAP, BACKLOG, CHANGELOG, CONTRIBUTING, SECURITY, architecture, style guidance, AGENTS size guard, rendered-output test, lint, test, and production build all exist and pass local validation.

  Acceptance evidence: `npm run validate` passed locally. The run included `npm run agents:check` with `AGENTS.md` at 7,073 bytes out of the 32,000 byte limit, `npm run lint`, `npm run build`, and `node --test tests/*.test.mjs` with one passing rendered-output test. A private-content scan over public repository files found no matches for the sensitive reference-campaign terms identified during review. Residual uncertainty: this is a local scaffold only; no production deployment, AittaDB adapter, owner setup flow, or campaign publication was attempted.

### Retired Planning Umbrellas

These entries were removed because their scopes could not finish independently. Their preservation here does not claim that the underlying product work is complete; each retirement mapping identifies the focused current-plan replacements.

- **TASK-005:** Define campaign and phase configuration contracts. DoD: campaign identity, publication state, phase status, country eligibility, amount-rule, aggregate-display, and setup-completion contracts are typed and documented; tests cover unpublished/public visibility, restricted-country eligibility, amount minimum/increment validation, and no campaign-specific defaults.

  Retirement mapping: Replaced by `TASK-029`, `TASK-039`, and `TASK-040`.

- **TASK-026:** Add the owner campaign workspace. DoD: authorized owners receive setup, package, participant, indication, founder-application, reconciliation, export, audit, and manual-notification navigation based only on currently allowed actions; non-owners receive no owner links or state; every owner HTML control has an equivalent hypermedia action and focused authorization tests.

  Retirement mapping: Replaced by `TASK-028` and `TASK-032` through `TASK-038`.

- **TASK-002:** Define the framework-independent domain model and repository interfaces. DoD: campaign, phase, content version, participant, acknowledgment, indication, founder application, aggregate, audit, notification, and storage-adapter contracts are documented and typed; no framework or AittaDB HTTP details leak into domain types; focused unit tests cover money integer handling and initial validation boundaries.

  Retirement mapping: Replaced by `TASK-004` through `TASK-012`.

- **TASK-003:** Implement the deterministic in-memory/test storage adapter. DoD: adapter supports the domain repositories needed for setup, package, participant, indication, founder, aggregate, acknowledgment, audit, and notification flows; contract tests prove idempotency, uniqueness, immutable history, non-disclosure failures, and aggregate consistency.

  Retirement mapping: Replaced by `TASK-013` through `TASK-019`.

- **TASK-055:** Diagnose and harden hosted OAuth provider availability. Depends on: `TASK-051`. DoD: the owner connection distinguishes only fixed non-secret discovery failure phases in private operator evidence, the deployed Worker can reach and validate the exact configured acceptance issuer without weakening endpoint, redirect, content-type, size, or grant checks, no URL query, credential, token, response body, identity, or exception enters logs or public output, and focused tests plus full validation pass.

  Retirement mapping: Replaced by `TASK-098` and `TASK-099`.

- **TASK-030:** Prove the configured AittaDB OAuth client path. Depends on: `TASK-055`. DoD: the selected AittaDB deployment advertises Authorization Code with PKCE, a confidential Investor App client has only the required storage scopes and exact redirect URIs, secrets remain hosted-only, and a development callback obtains and validates a scoped token without logging credentials.

  Retirement mapping: Replaced by `TASK-100` and `TASK-101`.

- **TASK-059:** Prove the production AittaDB `StorageAdapter` against hosted acceptance. Depends on: `TASK-088`. Blocked by: the AittaDB project must publish the accepted atomic storage transaction action on its disposable acceptance deployment. DoD: the disposable deployment advertises the accepted transaction contract, a least-privilege service-client-bound adapter passes the unchanged hosted authorization, consistency, uniqueness, history, pagination, idempotency, quota, rollback, restart, and non-disclosure matrix, all fixtures and credentials are removed or intentionally retained outside Git, and documentation plus full validation pass.

  Retirement mapping: Replaced by `TASK-131` and `TASK-132`.

- **TASK-072:** Wire owner founder review. Depends on: `TASK-071`. DoD: the configured owner can page and inspect opaque founder-review resources backed by the same persistent applications while subjects stay out of URLs and public output; anonymous, non-owner, malformed cursor, oversized page, missing, and restart paths remain bounded and non-disclosing; HTML/hypermedia parity tests and full validation pass.

  Retirement mapping: Replaced by `TASK-133` and `TASK-134`.

- **TASK-075:** Wire owner indication moderation. Depends on: `TASK-067`, `TASK-073`, `TASK-097`. DoD: the configured owner can page, inspect, and reject an active indication with a bounded participant-visible reason through AittaDB-backed atomic indication/aggregate/audit/notification persistence; rejection removes the public contribution exactly once, foreign and stale requests disclose nothing and change nothing, and HTML/hypermedia, retry, rollback, restart, and full validation tests pass.

  Retirement mapping: Replaced by `TASK-102`, `TASK-103`, `TASK-104`, and `TASK-135`.

- **TASK-077:** Wire owner audit and manual-notification history. Depends on: `TASK-067`, `TASK-068`, `TASK-069`, `TASK-073`, `TASK-075`. DoD: bounded persistent audit and manual-notification collections expose only owner-authorized HTML/hypermedia resources, copy evidence remains distinct from the owner-entered sent marker, expected campaign/package/indication/moderation transitions appear once across retries and restarts, private templates never enter public errors or logs, and focused tests plus full validation pass.

  Retirement mapping: Replaced by `TASK-105`, `TASK-106`, and `TASK-136`.

- **TASK-078:** Wire owner review exports. Depends on: `TASK-068`, `TASK-069`, `TASK-089`, `TASK-072`, `TASK-073`, `TASK-076`, `TASK-077`. DoD: owner-only CSV review and versioned JSON backup stream bounded current AittaDB-backed campaign, package, participant, founder, indication, aggregate, and audit projections with formula neutralization and guaranteed immutable audit evidence; anonymous, non-owner, stale, oversized, looping-page, cancellation, retry, and restart paths expose no partial download or credential; and full validation passes.

  Retirement mapping: Replaced by `TASK-107` and `TASK-108`.

- **TASK-079:** Coordinate participant account-deletion requests. Depends on: `TASK-064`, `TASK-089`, `TASK-071`, `TASK-073`, `TASK-077`. DoD: one retry-stable AittaDB-backed application operation marks deletion requested, withdraws every active founder and investment interest, updates aggregates, and appends closed audit evidence atomically or leaves all state unchanged; access is immediately blocked according to policy, marketing state remains independent, and partial failure, retry, concurrency, restart, and non-disclosure tests plus full validation pass.

  Retirement mapping: Replaced by `TASK-109` and `TASK-110`.

- **TASK-080:** Prove the runtime-only AittaDB credential boundary. Depends on: `TASK-059`, `TASK-068`, `TASK-069`, `TASK-093`, `TASK-094`, `TASK-072`, `TASK-096`, `TASK-074`, `TASK-075`, `TASK-076`, `TASK-077`, `TASK-078`, `TASK-079`. DoD: source and artifact scans contain no instance credential or token; browser network, HTML, JSON, downloads, redirects, CSP, errors, and Worker logs expose none; the AittaDB service client has no redirect URI or browser origin and only required storage scopes; anonymous clients cannot call its namespace; secret rotation invalidates old credentials without data loss; and focused security tests plus full validation pass.

  Retirement mapping: Replaced by `TASK-111` through `TASK-114`.

- **TASK-081:** Complete hosted pre-registration acceptance. Depends on: `TASK-080`. DoD: one exact acceptance deployment proves public campaign, first registration, immediate package access, acknowledgment, personal and company indications, founder application, edits, withdrawals, reactivation, public totals, owner setup/package/review/moderation/reconciliation/notifications/exports, account deletion, HTML/hypermedia parity, two-user isolation, restart persistence, concurrency, recovery, responsive accessibility, and credential-free logs using only synthetic data; all fixtures are removed or retained intentionally and full validation passes.

  Retirement mapping: Replaced by `TASK-137` through `TASK-151`.

- **TASK-082:** Prepare the AittaDB production Investor App runtime. Depends on: `TASK-081`. Blocked by: explicit owner approval for production AittaDB service-client registration, hosted-secret updates, and an unpublished `invest.aittadb.com` deployment. DoD: a dedicated service client with no origins or redirects and only required storage scopes is stored solely in the production Sites environment, exact origins, quotas, privacy contact, owner identity, bindings, and rollback version are verified, the validated source is deployed without publishing a campaign or mutating unrelated production data, and no secret or private setting enters Git or retained evidence.

  Retirement mapping: Replaced by `TASK-120` through `TASK-122`.

- **TASK-083:** Configure and review the AittaDB pre-registration campaign. Depends on: `TASK-082`. Blocked by: the owner must supply or approve the real public copy, private package, acknowledgment, phase, country, amount, privacy, retention, and legal-boundary settings. DoD: the configured owner completes setup through the production owner UI, reviews public preview and every enabled private section, confirms non-binding and privacy wording, verifies no empty or synthetic content remains, keeps the campaign unpublished, and retained evidence contains no private package text, participant data, credential, or internal strategy.

  Retirement mapping: Replaced by `TASK-123`, `TASK-124`, and `TASK-152`.

- **TASK-084:** Publish `invest.aittadb.com` for pre-registration. Depends on: `TASK-083`. Blocked by: explicit owner approval of the exact production preview and publication action. DoD: the owner explicitly publishes the reviewed campaign, signed-out and signed-in production smoke tests prove the public, participant, and owner boundaries without synthetic submissions or unrelated data mutation, monitoring and rollback checks pass, the deployed source and environment revisions are recorded without secrets, and no merge to `main` occurs before PR approval.

  Retirement mapping: Replaced by `TASK-125`, `TASK-126`, and `TASK-153`.

- **TASK-085:** Remove temporary OAuth acceptance administration. Depends on: `TASK-030`. DoD: the proof client grants are revoked and the client is disabled or removed, its hosted secret and unused proof-only configuration are removed from the Investor acceptance environment, the temporary AittaDB administrator subject is removed by the AittaDB project, anonymous and former-admin access fail as expected, no unrelated acceptance setting or data changes, and retained evidence contains no identity, credential, token, or private environment value.

  Retirement mapping: Replaced by `TASK-127` through `TASK-130`.
